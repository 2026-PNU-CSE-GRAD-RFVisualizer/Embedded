# 3DGS 기반 공간 데이터 시각화 - 임베디드 파트 구현 계획

작성일: 2026-06-25  
수정일: 2026-06-25  
2차 수정: 구매한 Waveshare 4inch 480×800 8080 병렬 LCD와 미구매 IMU 선정 전략 반영  
최종 수정: ESP32-S3-DEVKITC-1-N8R8 보드 선정, LCD 버퍼링 전략, 핀맵 검증 절차 반영  
담당: 이나영  
대상 기간: 2026년 6월 말 ~ 9월

## 1. 문서 목적

본 문서는 졸업과제 기획안과 착수보고서에서 정의한 임베디드 역할을 실제 구현 가능한 작업 단위로 구체화한다.

그래픽스 파트의 최신 파이프라인과 핸드헬드 디바이스 설계를 반영하여 임베디드 시스템을 다음 두 종류의 장치로 분리한다.

1. **고정 ESP32 RSSI 측정 노드**
   - 실내 여러 위치에서 특정 AP 또는 기준 신호원의 RSSI를 반복 측정한다.
   - 측정값을 필터링하고 MQTT로 백엔드에 전송한다.
   - 통신 장애가 발생해도 측정 데이터를 최대한 보존한다.
   - 노드 위치와 상태 정보를 함께 제공하여 RF Field 검증 및 위치 갱신 실험에 활용한다.

2. **핸드헬드 ESP32-S3-DEVKITC-1-N8R8 표시 장치**
   - 스마트 IMU(BNO085/BNO080 우선, BNO055 차선)로 장치 방향을 추정해 PC Viewer에 전송한다.
   - 평상시에는 위치를 고정하고, IMU 기반 회전값만 이용해 시야 방향을 실시간으로 갱신한다.
   - 위치 갱신 버튼을 눌렀을 때만 현재 RSSI 정보 또는 위치 갱신 요청을 보내고, PC/백엔드가 계산한 Camera Position으로 순간이동하듯 갱신한다.
   - 구매 완료한 Waveshare 4inch 480×800 Resistive Touch LCD(NT35510, 16-bit 8080 Parallel)에 PC 렌더링 화면을 JPEG 수신·디코딩 후 표시한다.
   - 전체 RGB565 풀프레임 삼중버퍼를 기본 구조로 사용하지 않고, JPEG 수신 더블버퍼와 RGB565 Line/Tile 더블버퍼를 우선 사용한다.

핸드헬드 장치는 3DGS 렌더링, Sionna RT 계산, 무거운 위치 추정 연산을 직접 수행하지 않는 **Thin Client**로 구현한다. 최종 보드는 **ESP32-S3-DEVKITC-1-N8R8**로 선정하며, ESP32-S3는 센서 입력, 버튼 이벤트, 네트워크 송수신, LCD 표시를 담당하고, 위치 추정과 3DGS Viewer 카메라 갱신은 PC 또는 백엔드에서 처리한다.

---

## 2. 구현 범위

본 프로젝트의 임베디드 범위는 성공 필수 기능과 고위험 시연 기능을 분리한다. LCD 스트리밍이나 RSSI 기반 위치 추정이 지연되더라도 고정 노드 RSSI 수집 파이프라인이 완성되도록 우선순위를 둔다.

### 2.1 MVP-A: 반드시 성공해야 하는 핵심 범위

- 고정 ESP32 노드 3개 이상
- 한 개의 고정 AP 또는 기준 BSSID를 대상으로 RSSI 측정
- FreeRTOS 기반 측정·통신·상태 관리 태스크 분리
- Moving Average 또는 Median Filter 적용
- MQTT 기반 RSSI 및 장치 상태 전송
- `node_id`, `seq`, `timestamp`, BSSID, 좌표, 상태 코드 포함
- Wi-Fi/MQTT 자동 재연결
- 전송 실패 데이터의 제한적 RAM Ring Buffer 저장 및 재전송
- Watchdog과 장치 상태 코드
- FPS, 지연시간, RSSI 변동, 패킷 손실 및 재연결 시간 측정
- 2시간 이상 안정성 시험

### 2.2 MVP-B: 핸드헬드 1차 시연 범위

- ESP32-S3-DEVKITC-1-N8R8 기반 핸드헬드 장치
- 스마트 IMU 모듈 연동
  - 1순위: BNO085/BNO080 계열
  - 2순위: BNO055 계열
  - 순수 6축 IMU는 개발 시간이 부족할 때 비추천하며, 이미 보유한 경우에만 대체안으로 사용
- IMU에서 Quaternion 또는 Rotation Vector를 직접 받아 UDP로 전송
- 장치 회전에 따른 PC Viewer 시야 방향 갱신
- 방향 재중심화 버튼
- 위치 갱신 버튼
- 버튼 입력 시 PC Viewer에 Position Update 요청 전송
- 실제 위치는 연속 추적하지 않고, 버튼 입력 시에만 갱신
- PC 테스트 수신기에서 방향과 버튼 이벤트 확인

### 2.3 MVP-C: 성공하면 좋은 고위험 시연 범위

- 구매 완료한 Waveshare 4inch 480×800 Resistive Touch LCD 연동
  - Driver: NT35510
  - LCD Interface: 16-bit 8080 Parallel
  - Touch Controller: XPT2046, SPI
- TCP 기반 JPEG 프레임 수신
- JPEG 디코딩 후 RGB565로 변환
- LCD 컨트롤러 GRAM으로 화면 데이터 전송
- Full Framebuffer Triple Buffer는 기본 전략에서 제외
- JPEG Buffer A/B + RGB565 Line/Tile Buffer A/B 기반 파이프라인 우선
- 1차 목표: 480×800 세로 또는 800×480 가로 모드에서 5 FPS 이상
- 도전 목표: 동일 해상도에서 평균 10 FPS
- JPEG 품질 50, 65, 80 벤치마크
- 프레임 누적 지연 방지를 위한 최신 프레임 우선 처리
- LCD 실패 시 PC Viewer 화면 기반 시연으로 즉시 전환하는 Plan B 유지

### 2.4 MVP 이후 확장 범위

- 버튼 입력 시 핸드헬드 위치 자동 갱신
- RSSI Fingerprinting 기반 위치 후보 매칭
- 구역 기반 Position Snap
- RSSI 거리 추정 기반 다변측량 또는 삼각측량 비교 실험
- 고정 노드 수 증가에 따른 위치 추정 안정성 시험
- 실제 RSSI와 Sionna RT 결과의 자동 보정
- 배터리 구동과 전력 관리
- OTA Firmware Update

### 2.5 현재 구현하지 않을 항목

- ESP32에서 3DGS 또는 Sionna RT 직접 실행
- 핸드헬드의 연속 6DoF 위치 추적
- IMU 적분만으로 사람의 이동 거리와 공간 좌표를 계산하는 것
- 순수 6축 IMU의 Raw Data를 직접 튜닝해 완벽한 장시간 Quaternion 추적을 보장하는 것
- RSSI 기반 거리 추정만으로 정확한 실시간 좌표를 보장하는 것
- 여러 높이의 3D Volume 데이터를 ESP32에서 직접 처리하는 것
- 고정 노드 기반 위치 추정 알고리즘을 초기 단계에서 최종 확정하는 것

---

## 3. 전체 임베디드 구조

```text
                    [고정 RSSI 측정 노드 N개]

특정 AP / 기준 BSSID RSSI 측정
    ↓
필터링 / 이상치 제거
    ↓
측정 Queue
    ↓
MQTT Publish
    ↓
백엔드 수집·동기화
    ↓
실제 RSSI 검증 데이터 / RF Field 보정 데이터


                    [핸드헬드 ESP32-S3-DEVKITC-1-N8R8]

스마트 IMU → Quaternion / Rotation Vector ─ UDP ─→ PC Viewer
버튼 → Recenter Event ─────────────── UDP ──→ PC Viewer
버튼 → Position Update Request ────── UDP ──→ PC Viewer / Backend
주변 기준 노드 RSSI Scan(확장) ────── UDP/MQTT ─→ Backend
                                                       │
                                                       │ 위치 후보 계산
                                                       │ Camera Position 갱신
                                                       │ 3DGS + Heatmap 렌더링
                                                       ↓
Waveshare 480×800 LCD ← JPEG Decode ← TCP Frame 수신 ← JPEG Streaming
```

고정 노드의 MQTT 계측 경로와 핸드헬드의 실시간 제어·영상 경로는 분리한다. 한 경로의 장애가 다른 경로를 정지시키지 않도록 각각 독립적으로 구현한다.

핸드헬드 위치 갱신은 연속 추적이 아니라 **버튼 기반 이산 갱신**으로 제한한다. 평상시에는 Camera Position을 고정하고, IMU 기반 회전값으로 Camera Orientation만 갱신한다. 위치 갱신 버튼을 누르면 현재 RSSI 정보 또는 위치 갱신 요청을 PC/백엔드에 보내고, 계산된 위치 후보로 Camera Position을 순간이동하듯 갱신한다.

---

## 4. 하드웨어 구성 계획

## 4.1 고정 RSSI 측정 노드

필수 구성:

- ESP32 계열 보드
- 안정적인 USB 전원 또는 5 V 어댑터
- 상태 확인용 LED
- 필요 시 리셋 버튼

노드별로 다음 정보를 고정 설정한다.

- `node_id`
- 설치 위치의 실제 좌표
- 측정 대상 AP의 SSID/BSSID
- MQTT Broker 주소
- 측정 주기
- 좌표계 ID 또는 공간 기준점 정보

AP 식별에는 SSID보다 **BSSID를 우선 사용**한다. 동일 SSID를 사용하는 여러 AP가 있을 때 측정 대상이 섞이는 문제를 방지하기 위해서다.

고정 노드는 기본적으로 RF Field 검증용 RSSI 측정 노드로 사용한다. 이후 핸드헬드 위치 갱신 실험에서는 기준 신호원 또는 위치 후보 매칭 기준점으로 확장할 수 있다.

## 4.2 핸드헬드 표시 장치

현재 구매 완료된 LCD를 기준으로 핸드헬드 장치를 설계한다. IMU는 아직 구매 전이므로, 순수 6축 모듈보다 센서 퓨전 결과를 직접 제공하는 스마트 IMU를 우선 선정한다.

기준 구성:

- ESP32-S3-DEVKITC-1-N8R8
  - Flash: 8 MB
  - PSRAM: 8 MB
  - USB-C 기반 개발 및 디버깅
  - 공식 DevKit 계열 보드이므로 ESP-IDF 예제와 핀맵 자료를 우선 활용
- Waveshare 4inch Resistive Touch LCD
  - 제품번호: EPXPMJPJ
  - 모델명: WVS-17143
  - Driver: NT35510
  - Resolution: 480×800
  - LCD Interface: 16-bit 8080 Parallel
  - Touch Controller: XPT2046
  - Touch Interface: SPI
  - Touch Type: 4-wire resistive
  - Logic Level: 3.3 V
- 스마트 IMU 모듈
  - 1순위: BNO085/BNO080 계열
  - 2순위: BNO055 계열
  - 순수 6축 MPU6050/ICM 계열은 직접 필터 튜닝 시간이 크므로 후순위
- 위치 갱신 버튼
- 방향 재중심화 버튼
- 연결 및 오류 상태 표시 수단
- 안정적인 외부 전원

이 LCD는 RGB 생패널이 아니라 NT35510 컨트롤러를 포함한 8080 병렬 LCD이므로, 화면 유지 자체를 ESP32-S3가 계속 스캔하는 구조는 아니다. 그러나 480×800 전체 화면을 매 프레임 갱신하면 데이터량은 여전히 크다.

```text
480 × 800 × 2 byte = 768,000 byte/frame
5 FPS  ≈ 3.8 MB/s 이상
10 FPS ≈ 7.7 MB/s 이상
```

따라서 LCD는 구매한 제품을 사용하되, 목표를 다음처럼 둔다.

| 구분 | 목표 | 판단 |
|---|---|---|
| 필수 | 단색 화면, 테스트 이미지, 상태 UI 출력 | 반드시 성공 |
| 1차 | PC가 보낸 JPEG를 480×800 또는 800×480 모드로 5 FPS 이상 표시 | 현실 목표 |
| 도전 | 동일 해상도에서 평균 10 FPS | 성공하면 가산점 |
| 대체 | LCD 스트리밍 실패 시 PC Viewer 화면으로 시연 | Plan B |

하드웨어 연결 전에 아래 항목을 먼저 확인한다.

- ESP32-S3 보드의 실제 사용 가능 GPIO 수
- 16-bit 8080 병렬 데이터 핀 D0~D15 배치 가능 여부
- LCD 제어핀 CS, DC/RS, WR, RD, RST, BL 배치 가능 여부
- Touch SPI 핀과 IMU I2C/SPI 핀의 충돌 여부
- Flash, PSRAM, USB, JTAG, Strapping Pin과의 충돌 여부
- 16-bit 병렬이 어려울 경우 8-bit 모드 또는 다른 전송 방식이 가능한지 여부
- LCD NT35510 드라이버 예제 확보 여부
- JPEG Decoder가 라인/타일 단위 출력을 지원하는지 여부
- 버튼 GPIO Debounce 처리 방식

터치 기능은 초기 MVP에서 필수로 사용하지 않는다. 초기 시연은 물리 버튼 기반 Position Snap과 Recenter를 우선 구현하고, 터치는 안정화 이후 위치 후보 선택 UI로 확장한다.

IMU는 위치 센서가 아니라 자세 및 회전 추정 센서로 사용한다. 즉, IMU로 사람의 이동 좌표를 연속 계산하지 않고, 핸드헬드 장치의 시야 방향만 추정한다. 위치 갱신은 버튼 입력과 RSSI 기반 위치 후보 계산을 통해 별도로 수행한다.

### 4.3 ESP32-S3-DEVKITC-1-N8R8 핀맵 검증 원칙

최종 보드는 ESP32-S3-DEVKITC-1-N8R8로 선정한다. 이 보드는 PSRAM 8 MB가 포함된 DevKitC-1 계열이므로 JPEG 수신 버퍼, LCD 전송 버퍼, 네트워크 처리에 필요한 메모리 여유를 확보하기 쉽다. 다만 Waveshare LCD가 16-bit 8080 병렬 인터페이스를 사용하므로 실제 구현 전 핀맵 검증을 최우선으로 수행한다.

핀 사용 우선순위는 다음과 같다.

| 우선순위 | 기능 | 필요 핀 | 처리 방침 |
|---:|---|---:|---|
| 1 | LCD 8080 데이터 | D0~D15, 16핀 | 반드시 우선 배치 |
| 2 | LCD 제어 | CS, DC/RS, WR, RD, RST, BL | RD는 읽기 미사용 시 고정 가능 여부 검토 |
| 3 | IMU | I2C SDA/SCL 또는 SPI | BNO085/BNO080 모듈 인터페이스에 맞춤 |
| 4 | 버튼 | Position, Recenter | 내부 Pull-up 또는 외부 저항 적용 |
| 5 | 터치 | XPT2046 SPI | MVP 이후 후순위 |
| 6 | 디버그 | UART/USB 로그 | 가능하면 확보 |

초기 연결 전략은 다음과 같다.

1. LCD 출력만 먼저 연결한다.
2. 터치는 연결하지 않고 버튼 입력으로 대체한다.
3. LCD 단색 출력, 사각형 출력, 테스트 이미지 출력이 안정화된 뒤 IMU를 추가한다.
4. LCD + IMU + 버튼이 안정화된 뒤 터치 연결을 검토한다.
5. 16-bit 8080 병렬 핀 배치가 불가능하면 8-bit 모드 가능 여부를 확인한다.
6. 8-bit 모드도 어렵거나 FPS가 지나치게 낮으면 PC Viewer 기반 Plan B로 전환한다.

---

---

## 5. 고정 ESP32 노드 Firmware 설계

## 5.1 FreeRTOS 태스크 구성

초기 구현에서는 태스크를 너무 잘게 쪼개기보다 측정, 통신, 연결, 상태 관리를 중심으로 구성한다. 이후 필요할 때 필터링 또는 저장 태스크를 분리한다.

| 태스크 | 역할 | 우선순위 원칙 |
|---|---|---|
| `MeasurementTask` | 특정 AP/BSSID RSSI 측정, 간단 필터링, 샘플 생성 | 주기 정확성 우선 |
| `MqttTask` | Queue의 측정값과 상태 전송, 실패 시 RAM Ring Buffer 처리 | 네트워크 지연이 측정을 막지 않도록 분리 |
| `ConnectionTask` | Wi-Fi/MQTT 연결 및 재연결, Event Group 업데이트 | 연결 상태 변화 시 동작 |
| `HealthTask` | Heap, Queue, 오류 횟수, Watchdog, Reset Reason 관리 | 낮은 주기로 실행 |

필요 시 확장 태스크:

| 태스크 | 역할 | 분리 조건 |
|---|---|---|
| `SignalFilterTask` | 평균·중간값·이상치 제거 | 필터 계산이 복잡해지거나 비교 실험이 필요할 때 |
| `StorageTask` | 전송 실패 데이터 임시 저장 | RAM Ring Buffer만으로 부족할 때 |

태스크 간 데이터 전달은 공유 전역 변수보다 FreeRTOS Queue와 Event Group을 우선 사용한다.

```text
MeasurementTask
    ↓ publish_queue
MqttTask
```

필터링 태스크를 분리하는 경우:

```text
MeasurementTask
    ↓ raw_sample_queue
SignalFilterTask
    ↓ publish_queue
MqttTask
```

MQTT 전송이 지연되더라도 RSSI 측정 태스크가 멈추지 않아야 한다.

## 5.2 RSSI 측정 및 필터링

초기 측정 절차:

1. 대상 BSSID의 RSSI를 일정 주기로 수집한다.
2. 한 번의 결과만 사용하지 않고 짧은 구간의 여러 샘플을 모은다.
3. 비정상 범위와 급격한 단일 튐을 제거한다.
4. 필터링 전·후 값을 모두 시험 로그에 남긴다.

초기 권장 설정:

| 항목 | 초기값 | 조정 기준 |
|---|---:|---|
| 샘플 주기 | 1 s부터 시작 | Wi-Fi 처리 부하와 MQTT 지연 확인 후 단축 |
| 전송 주기 | 1 s | 백엔드 Time Window와 합의 |
| Moving Average Window | 5개 | 반응성과 안정성 비교 |
| Median Window | 5개 | 순간 이상치 제거 성능 비교 |
| 유효 RSSI 범위 | -100~-10 dBm | 실제 실험 후 확정 |

측정 대상 AP가 ESP32가 접속 중인 AP와 동일한 경우에는 전체 Wi-Fi Scan을 반복하기보다 연결된 AP의 RSSI를 조회하는 방식을 우선 검토한다. 전체 Scan은 통신 안정성에 영향을 줄 수 있으므로 주변 AP 또는 기준 노드를 확인해야 하는 경우에만 제한적으로 사용한다.

Moving Average와 Median Filter를 무조건 동시에 적용하지 않는다. 동일 위치 반복 측정 결과를 비교해 더 적합한 방식을 기본값으로 결정한다.

## 5.3 MQTT 데이터 형식

권장 Topic:

```text
rfmap/nodes/{node_id}/measurement
rfmap/nodes/{node_id}/status
rfmap/nodes/{node_id}/event
```

측정 메시지 예시:

```json
{
  "schema_version": 1,
  "node_id": "node-01",
  "seq": 15234,
  "timestamp_ms": 1782384000123,
  "local_uptime_ms": 3600000,
  "time_synced": true,
  "ap_bssid": "AA:BB:CC:DD:EE:FF",
  "ap_channel": 6,
  "rssi_raw_dbm": -62,
  "rssi_filtered_dbm": -60.8,
  "rssi_stddev_dbm": 2.1,
  "sample_count": 5,
  "coord_frame_id": "room_3dgs_v1",
  "x_m": 1.25,
  "y_m": 3.40,
  "z_m": 1.30,
  "status": 0
}
```

상태 메시지 예시:

```json
{
  "schema_version": 1,
  "node_id": "node-01",
  "timestamp_ms": 1782384000123,
  "local_uptime_ms": 3600000,
  "time_synced": true,
  "wifi_rssi_dbm": -48,
  "free_heap_bytes": 180000,
  "publish_queue_depth": 2,
  "buffered_record_count": 0,
  "drop_count": 0,
  "reconnect_count": 1,
  "boot_count": 3,
  "reset_reason": "POWERON_RESET",
  "error_flags": 0
}
```

`seq`는 메시지 누락과 중복을 판별하는 기준으로 사용한다. `timestamp_ms`의 생성 방식은 백엔드와 합의해야 하며, NTP 동기화 전에는 별도의 `time_synced` 상태를 제공한다.

MQTT QoS 권장:

| 메시지 | QoS | 이유 |
|---|---:|---|
| measurement | 0 | 주기적으로 계속 들어오므로 지연보다 최신성이 중요함 |
| status | 0 또는 1 | 상태 확인 목적이며 중요도에 따라 선택 |
| event/error | 1 | 장애 이벤트는 누락을 줄이는 것이 중요함 |
| LWT | 1 | 노드 오프라인 상태 표시 |

## 5.4 장애 대응

| 장애 | 동작 |
|---|---|
| Wi-Fi 연결 끊김 | 지수 Backoff 기반 재연결, 측정 지속 |
| MQTT Broker 연결 끊김 | Queue 또는 RAM Ring Buffer에 임시 저장 |
| Buffer 가득 참 | 가장 오래된 데이터부터 폐기하고 Drop Count 증가 |
| 장시간 태스크 정지 | Task Watchdog으로 재시작 |
| 잘못된 AP 측정 | BSSID 불일치 데이터 폐기 |
| 시간 동기화 실패 | 상대 시간과 `time_synced=false` 전송 |
| 전원 재부팅 | Boot Count 및 Reset Reason 상태 메시지 전송 |

재전송 데이터가 최신 실시간 데이터를 계속 밀어내지 않도록, 복구 후에는 최신 데이터와 과거 데이터를 구분해 제한된 속도로 전송한다. MVP에서는 Flash에 측정 데이터를 매초 저장하지 않는다. Flash 저장은 설정값, Boot Count, 간단한 오류 정보 정도로 제한하고, 측정 데이터는 RAM Ring Buffer를 기본으로 한다.

---

## 6. 핸드헬드 ESP32-S3-DEVKITC-1-N8R8 Firmware 설계

## 6.1 FreeRTOS 태스크 구성

| 태스크 | 역할 |
|---|---|
| `ImuTask` | 스마트 IMU에서 Quaternion 또는 Rotation Vector 수신, 필요 시 Recenter 보정 |
| `ControlTxTask` | 최신 방향값과 버튼 상태 UDP 전송 |
| `PositionScanTask` | 위치 갱신 버튼 입력 시 주변 기준 노드 RSSI 측정 또는 위치 갱신 요청 생성 |
| `VideoRxTask` | TCP 연결, Header 및 JPEG Payload 수신 |
| `JpegDecodeTask` | 최신 JPEG 프레임 디코딩 |
| `DisplayTask` | RGB565 데이터를 NT35510 LCD로 8080 병렬 전송 |
| `InputTask` | Position Update 및 Recenter 버튼 Debounce |
| `ConnectionTask` | Wi-Fi, UDP, TCP 연결 상태 관리 |
| `HealthTask` | FPS, Heap, PSRAM, 오류, Watchdog 관리 |

순수 6축 IMU를 사용하게 되는 경우에만 `ImuTask` 내부에 Gyroscope Bias Calibration, Madgwick/Mahony 필터, Drift 측정 기능을 추가한다. 스마트 IMU를 사용하는 경우 ESP32-S3는 센서 퓨전 알고리즘을 직접 구현하지 않고, 센서가 제공하는 Quaternion 또는 Rotation Vector를 읽어 전송하는 데 집중한다.

영상 경로는 다음과 같이 구성한다.

```text
TCP 수신
    ↓
JPEG Buffer A/B
    ↓
JPEG Decode
    ↓
RGB565 Line/Tile Buffer A/B
    ↓
NT35510 LCD GRAM 전송
```

영상 수신 속도가 디코딩 속도보다 빠르면 과거 프레임을 쌓지 않고 가장 최신의 완성 프레임을 우선한다. TCP 특성상 오래된 프레임이 소켓 버퍼에 누적될 수 있으므로, PC Viewer 쪽도 최신 프레임만 보내도록 송신 정책을 맞춘다.

풀프레임 RGB565 삼중버퍼는 기본 전략으로 사용하지 않는다. 480×800 RGB565 프레임 1장은 약 768 KB이며, 삼중버퍼는 약 2.3 MB를 차지한다. 용량상 PSRAM에 올릴 수는 있지만, 이 LCD는 RGB 생패널이 아니라 NT35510 내부 GRAM에 최종 화면을 기록하는 구조이므로 ESP32-S3 쪽 풀프레임 버퍼를 여러 장 swap하는 이점이 작다. 따라서 기본 구조는 다음처럼 둔다.

```text
PSRAM:
  jpeg_buf_A  # TCP 수신 중
  jpeg_buf_B  # JPEG 디코딩 중 또는 최신 완성 프레임 보관

DMA 가능 내부 메모리 또는 LCD 전송에 적합한 버퍼:
  tile_buf_A  # LCD 전송 중
  tile_buf_B  # 다음 라인/타일 디코딩 중

LCD:
  NT35510 내부 GRAM  # 실제 표시용 화면 저장소
```

즉, ESP32-S3는 전체 화면을 계속 보관하는 장치라기보다, PC가 보낸 JPEG를 작은 라인/타일 단위 RGB565 데이터로 풀어서 LCD 컨트롤러 GRAM에 밀어 넣는 장치로 설계한다.

## 6.2 IMU 및 버튼 처리

초기 동작:

- 부팅 후 스마트 IMU 초기화
- 센서가 제공하는 Quaternion 또는 Rotation Vector를 읽음
- Orientation은 지속적으로 갱신
- Position은 버튼을 눌렀을 때만 갱신 요청
- Recenter 버튼으로 현재 방향을 기준 방향으로 설정
- 순수 6축 IMU를 사용할 경우에만 Gyroscope Bias Calibration과 필터 튜닝을 수행

권장 전송 주기:

| 항목 | 초기값 | 비고 |
|---|---:|---|
| IMU 내부 샘플링 | 50~100 Hz | 스마트 IMU 출력 주기 기준 |
| Quaternion/Rotation Vector 읽기 | 50~100 Hz | I2C/SPI 안정성 확인 |
| UDP 제어 패킷 전송 | 30~60 Hz | Viewer 반응성과 Wi-Fi 부하 비교 |
| 위치 갱신 RSSI Scan | 버튼 입력 시에만 | 연속 Scan 금지 |

스마트 IMU를 사용하더라도 실내 자기장 왜곡이나 센서 오차로 방향이 틀어질 수 있으므로 Recenter 버튼은 필수로 둔다. 시연 기준은 장시간 완전 무보정 추적이 아니라, 사용자가 필요할 때 자연스럽게 기준 방향을 재설정할 수 있는 구조로 잡는다.

Yaw Drift 또는 방향 튐은 실제 장치에서 1분, 5분, 10분 정지 시험을 수행해 기록한다. 순수 6축 IMU를 사용해야 하는 상황이 생기면, 해당 시험 결과를 기준으로 최종 시연 사용 여부를 결정한다.

## 6.3 UDP 제어 패킷

네트워크 바이트 순서와 구조체 Padding 문제를 피하기 위해 필드를 명시적으로 직렬화한다.

권장 논리 구조:

```cpp
struct HandheldControlPacket
{
    uint32_t magic;
    uint16_t version;
    uint16_t flags;
    uint32_t sequence;
    uint64_t timestamp_us;

    float quaternion_x;
    float quaternion_y;
    float quaternion_z;
    float quaternion_w;

    uint32_t position_request_seq;
};
```

`flags` 예시:

- Bit 0: Position Update 요청
- Bit 1: Orientation Recenter 요청
- Bit 2: IMU Calibration 완료
- Bit 3: 장치 오류 존재
- Bit 4: RSSI 기반 위치 후보 데이터 포함 또는 별도 전송 완료

PC Viewer는 순서가 뒤늦게 도착한 UDP 패킷을 폐기하고 가장 큰 `sequence`의 최신 상태만 사용한다.

## 6.4 버튼 기반 Position Update 설계

핸드헬드의 위치 갱신은 연속 6DoF 추적이 아니라 **버튼 기반 Position Snap**으로 설계한다.

기본 동작:

```text
평상시:
Camera Position 고정
IMU 방향값만 계속 전송
Viewer는 시야 방향만 회전

Position Update 버튼 입력:
핸드헬드가 위치 갱신 요청 전송
필요 시 주변 기준 노드 RSSI를 짧게 측정
PC/백엔드가 위치 후보 계산
Viewer가 Camera Position을 해당 후보 위치로 갱신
사용자 입장에서는 순간이동처럼 화면 위치가 바뀜
```

위치 갱신 방식은 다음 순서로 구현한다.

| 단계 | 방식 | 설명 | 우선순위 |
|---|---|---|---:|
| 1 | 수동/사전 정의 Position Snap | 버튼 입력 시 미리 정한 후보 위치로 이동 | 가장 안정적 |
| 2 | RSSI Fingerprinting | 현재 RSSI 패턴과 사전 측정 패턴을 비교해 가장 가까운 위치 선택 | 권장 |
| 3 | 구역 기반 Snap | 정확한 좌표 대신 문 앞, 중앙, 창가 등 구역 단위로 이동 | 안정적 |
| 4 | RSSI 거리 추정 기반 다변측량 | RSSI를 거리로 변환해 위치 계산 | 비교 실험 |

최종 시연에서는 정확한 연속 좌표 추정보다 안정적인 위치 후보 갱신을 우선한다. RSSI 기반 다변측량 또는 삼각측량은 결과가 충분히 안정적일 때만 사용하고, 실패해도 핵심 시연에 영향을 주지 않도록 분리한다.

## 6.5 핸드헬드 RSSI 위치 갱신 데이터

핸드헬드가 주변 기준 노드 RSSI를 측정할 수 있는 구조가 확정되면 아래 형식으로 전송한다.

```json
{
  "schema_version": 1,
  "device_id": "handheld-01",
  "seq": 380,
  "timestamp_ms": 1782384000456,
  "position_request_seq": 17,
  "coord_frame_id": "room_3dgs_v1",
  "rssi_vector": [
    {
      "ref_id": "node-01",
      "bssid": "AA:BB:CC:DD:EE:01",
      "rssi_dbm": -54,
      "channel": 6
    },
    {
      "ref_id": "node-02",
      "bssid": "AA:BB:CC:DD:EE:02",
      "rssi_dbm": -68,
      "channel": 6
    },
    {
      "ref_id": "node-03",
      "bssid": "AA:BB:CC:DD:EE:03",
      "rssi_dbm": -61,
      "channel": 6
    }
  ]
}
```

초기 구현에서는 이 데이터를 ESP32-S3가 직접 위치 좌표로 계산하지 않는다. PC 또는 백엔드가 RSSI Fingerprinting, 구역 Snap, 다변측량을 비교하고, 선택된 Camera Position만 Viewer에 반영한다.

## 6.6 TCP JPEG 프레임 프로토콜

초기 구현은 `길이 Header + JPEG Payload` 구조로 한다.

권장 Header:

```cpp
struct VideoFrameHeader
{
    uint32_t magic;
    uint16_t version;
    uint16_t header_size;
    uint32_t sequence;
    uint32_t payload_size;
    uint16_t width;
    uint16_t height;
    uint64_t render_timestamp_us;
    uint32_t payload_crc32;
};
```

수신 절차:

1. Magic 값이 나올 때까지 프레임 시작점을 동기화한다.
2. Header 전체를 수신한다.
3. `payload_size`의 상한을 검사한다.
4. JPEG Payload를 완전히 수신한다.
5. CRC32를 검사한다.
6. 정상 프레임만 Decoder Queue에 전달한다.
7. 연결이 끊기면 소켓을 정리하고 재연결한다.

초기 JPEG 조건:

- LCD Native Resolution: 480×800
- 시연 화면: 세로 480×800 또는 가로 800×480 모드 중 Viewer와 합의
- Pixel 출력: RGB565
- 1차 목표: 5 FPS 이상
- 도전 목표: 평균 10 FPS
- JPEG 품질: 50, 65, 80을 각각 벤치마크
- Frame Queue: 1~2개
- 전체 프레임버퍼보다 Line/Tile Buffer 기반 출력 우선
- 풀프레임 삼중버퍼는 기본 구조에서 제외

프레임 지연 누적을 막기 위해 PC Viewer와 다음 정책 중 하나를 합의한다.

1. **Pull 방식**
   - ESP32-S3가 `READY(sequence)`를 보내면 PC가 최신 프레임 1장을 전송한다.
   - ESP32-S3가 수신, 디코딩, 표시를 마친 뒤 다음 `READY`를 보낸다.

2. **서버 최신 프레임 유지 방식**
   - PC Viewer는 송신 큐에 과거 프레임을 쌓지 않는다.
   - ESP32-S3가 받을 수 있을 때 가장 최신 렌더링 결과만 전송한다.

## 6.7 메모리 관리

구매한 LCD는 NT35510 컨트롤러를 포함하므로 화면 데이터는 최종적으로 LCD 컨트롤러의 GRAM에 기록된다. 따라서 ESP32-S3가 RGB 생패널처럼 화면 유지용 Pixel Clock을 계속 공급할 필요는 없다. 다만 JPEG 수신, 디코딩, RGB565 변환, 8080 병렬 전송 과정에서 임시 버퍼가 필요하다.

PSRAM에는 큰 버퍼를 배치한다.

- JPEG 수신 버퍼 2개: `jpeg_buf_A`, `jpeg_buf_B`
- 네트워크 수신 임시 버퍼
- 필요 시 RGB565 Full Framebuffer 1개 이하
- 기본 전략에서는 Full Framebuffer 대신 Line Buffer 또는 Tile Buffer 사용

LCD 전송용 RGB565 Line/Tile Buffer는 가능한 한 DMA 전송에 적합한 메모리에 배치한다.

- `tile_buf_A`: LCD 전송 중인 라인/타일
- `tile_buf_B`: JPEG Decoder가 다음 라인/타일을 작성 중인 버퍼
- 권장 타일 크기: 16~32 line 단위부터 시작

내부 SRAM에는 시간 민감도가 높은 작은 데이터만 배치한다.

- IMU 상태
- UDP 제어 패킷
- 버튼 상태
- Queue Handle
- 연결 상태와 통계

모든 프레임마다 큰 메모리를 동적 할당·해제하지 않는다. 초기화 시 최대 크기의 버퍼를 확보하고 재사용한다. 480×800 RGB565 Full Framebuffer는 약 768 KB이므로, Double Buffer 또는 Triple Buffer는 반드시 필요한 경우가 아니면 사용하지 않는다. 특히 풀프레임 삼중버퍼는 약 2.3 MB를 차지하고 LCD GRAM 전송 단계에서 다시 전체 데이터를 밀어 넣어야 하므로 MVP 구조에서는 제외한다.

---

## 7. 파트 간 인터페이스

## 7.1 임베디드 ↔ 백엔드

임베디드가 제공할 항목:

- MQTT Topic 규칙
- 측정·상태 JSON Schema
- `node_id`, `seq`, `timestamp`, 좌표, BSSID
- 오류 코드와 재연결 상태
- 샘플링 및 필터링 방식
- `coord_frame_id`
- 핸드헬드 RSSI 위치 갱신 데이터 형식

백엔드에서 받아야 할 항목:

- MQTT Broker 주소와 인증 방식
- Time Window 크기
- Timestamp 허용 오차
- 메시지 보관 및 중복 처리 정책
- 장치 설정 변경 채널이 필요한지 여부
- 위치 후보 계산을 백엔드에서 수행할지 PC Viewer에서 수행할지 여부

## 7.2 임베디드 ↔ 그래픽스/PC

임베디드가 제공할 항목:

- UDP Quaternion/Control Packet
- Position Update 및 Recenter Event
- IMU 유효 상태와 Sequence
- 위치 갱신 요청 번호 `position_request_seq`
- 필요 시 RSSI Vector 또는 위치 후보 요청 메시지

그래픽스 파트에서 받아야 할 항목:

- JPEG TCP Server 주소와 Port
- UDP Pose 수신 Port
- 확정된 Quaternion 축 방향과 회전 순서
- Camera 좌표계 변환 규칙
- Video Frame Header와 최대 Payload 크기
- Viewer가 사용하는 화면 해상도와 JPEG 형식
- Position Snap 후보 좌표 목록
- 위치 갱신 요청을 받았을 때 Camera Position을 갱신하는 규칙

통합 전에 다음 좌표계 합의를 문서로 확정해야 한다.

- ESP32 IMU의 전방·우측·상단 축
- Quaternion의 성분 순서
- Right-handed/Left-handed 좌표계
- 초기 자세의 정의
- Recenter 적용 위치
- 3DGS 공간 좌표와 실제 방 좌표의 변환식
- Position Snap 후보 위치의 `x, y, z` 좌표

## 7.3 위치 갱신 인터페이스 합의

위치 갱신은 아래 중 하나의 방식으로 합의한다.

| 방식 | 임베디드 역할 | PC/백엔드 역할 | 비고 |
|---|---|---|---|
| 수동 Snap | 버튼 이벤트 전송 | 다음 후보 위치 또는 지정 위치로 이동 | 가장 단순 |
| Fingerprinting | 주변 기준 RSSI Vector 전송 | 사전 측정 DB와 비교 후 위치 후보 선택 | 권장 |
| 구역 Snap | RSSI 또는 버튼 이벤트 전송 | 가장 가까운 구역으로 이동 | 안정적 |
| 다변측량 | RSSI Vector 전송 | RSSI→거리 변환 후 위치 계산 | 비교 실험 |

초기 시연에서는 수동 Snap 또는 Fingerprinting을 우선한다. 다변측량은 RSSI 편차와 오차를 측정한 뒤 사용 여부를 결정한다.

---

## 8. 단계별 구현 계획

## 1단계 - 개발 환경과 하드웨어 검증

기간: 2026-06-25 ~ 2026-07-03

작업:

- ESP-IDF 또는 현재 팀 표준 개발 환경 확정
- 고정 ESP32 보드와 핸드헬드 ESP32-S3-DEVKITC-1-N8R8 보드 구분
- ESP32-S3-DEVKITC-1-N8R8 기준 Waveshare 4inch 480×800 LCD 핀맵 작성
- NT35510 8080 병렬 LCD 단독 예제 실행
- PSRAM 인식과 사용 가능 용량 확인
- 스마트 IMU 후보 선정 및 구매
  - BNO085/BNO080 우선
  - BNO055 차선
- MQTT Broker와 테스트 Publish 확인
- PC에서 테스트 TCP JPEG Server 준비 요청
- PC에서 UDP Pose 테스트 수신기 준비

완료 조건:

- ESP32에서 대상 AP RSSI 출력
- ESP32-S3에서 LCD 단색 및 테스트 이미지 출력
- ESP32-S3-DEVKITC-1-N8R8에서 LCD 16-bit 8080 병렬 핀 연결 가능성 확인
- 스마트 IMU에서 Quaternion 또는 Rotation Vector 출력 확인
- PSRAM 인식과 사용 가능 용량 확인
- 2026-07-15 전까지 LCD 예제가 불안정하면 PC Viewer 기반 Plan B로 전환 판단

## 2단계 - 고정 노드 최소 계측 기능

기간: 2026-07-04 ~ 2026-07-12

작업:

- BSSID 기준 RSSI 측정
- `MeasurementTask`, `MqttTask` 분리
- 기본 JSON 패킷 정의
- `node_id`, `seq`, `timestamp`, `coord_frame_id` 적용
- 1개 노드 MQTT 전송

완료 조건:

- 1시간 연속 측정 중 태스크 정지 없음
- 서버에서 Sequence 누락률 확인 가능
- 측정 중 MQTT 지연이 발생해도 Measurement Task가 유지됨

## 3단계 - 필터링과 멀티 노드 확장

기간: 2026-07-13 ~ 2026-07-22

작업:

- Moving Average와 Median Filter 비교
- 동일 위치 반복 측정 데이터 저장
- 노드 3개 이상 동시 동작
- 노드 위치와 상태 메시지 추가
- 백엔드 Time Window 동기화 시험

완료 조건:

- 필터 선택 근거를 그래프로 제시 가능
- 노드 3개 이상이 고유 ID로 동시에 전송
- 백엔드에서 동일 시간 구간의 프레임 생성 가능

## 4단계 - 고정 노드 장애 대응

기간: 2026-07-23 ~ 2026-07-31

작업:

- Wi-Fi/MQTT 자동 재연결
- 제한 크기 RAM Ring Buffer
- 재전송 및 Drop Count
- Watchdog
- Reset Reason, Free Heap, Reconnect Count 전송

완료 조건:

- AP 또는 Broker를 1분간 중단한 뒤 자동 복구
- 복구 후 새 데이터 전송 정상화
- Buffer Overflow 시 장치가 멈추지 않고 Drop Count만 증가

## 5단계 - 핸드헬드 IMU 방향 및 버튼 제어

기간: 2026-08-01 ~ 2026-08-08

작업:

- 스마트 IMU 초기화
- Quaternion 또는 Rotation Vector 수신
- UDP 제어 패킷 구현
- Position Update와 Recenter 버튼
- Sequence와 Timestamp 추가
- PC 테스트 수신기에서 자세 확인
- 순수 6축 IMU를 사용하게 된 경우에만 Bias Calibration 및 필터 튜닝 수행

완료 조건:

- 장치 회전 방향과 PC Camera 회전 방향이 일치
- 버튼 입력이 중복 없이 전달됨
- 1분, 5분, 10분 방향 안정성 측정값 확보
- Recenter 버튼으로 기준 방향 재설정 가능
- UDP 손실 시에도 최신 Orientation으로 복구

## 6단계 - 버튼 기반 Position Snap 1차 구현

기간: 2026-08-09 ~ 2026-08-15

작업:

- Viewer의 후보 Camera Position 목록 정의
- Position Update 버튼 입력 시 위치 갱신 요청 전송
- PC Viewer에서 후보 위치로 Camera Position 갱신
- 위치는 연속 이동이 아니라 순간이동 방식으로 처리
- 필요 시 후보 위치 순환 방식 또는 수동 지정 방식 구현

완료 조건:

- 버튼 입력 시 Viewer가 위치 갱신 요청을 수신함
- Position 버튼 미입력 시 위치가 유지됨
- 버튼 입력 시 Camera Position이 후보 위치로 갱신됨
- Orientation은 위치 갱신과 무관하게 계속 반영됨

## 7단계 - 핸드헬드 LCD·JPEG 단독 벤치마크

기간: 2026-08-16 ~ 2026-08-24

작업:

- NT35510 LCD 8080 병렬 출력 드라이버 안정화
- TCP 프레임 수신기 구현
- JPEG Buffer와 Decoder 연결
- RGB565 변환 후 LCD GRAM 전송
- Full Framebuffer 방식과 Line/Tile Buffer 방식 비교
- 풀프레임 삼중버퍼는 참고 실험으로만 고려하고 MVP 기본 구조에서는 제외
- JPEG 품질 50/65/80 비교
- PSRAM 사용량, 수신·디코딩·출력 시간 측정
- Pull 방식 또는 최신 프레임 유지 방식 검토

완료 조건:

- PC 테스트 영상이 480×800 또는 800×480 모드로 LCD에 표시됨
- 5 FPS 이상 가능 여부 확인
- 평균 10 FPS 도달 가능성 확인
- 프레임 손상과 연결 해제 시 자동 복구
- 품질별 프레임 크기와 지연시간 기록
- 터치 기능은 필수 조건에서 제외

이 단계는 전체 Viewer 통합 전에 반드시 완료한다. 구매한 LCD에서 5 FPS 이상이 어렵거나 드라이버가 불안정하면, 핸드헬드는 IMU 컨트롤러 역할만 수행하고 화면은 PC Viewer에서 출력한다.

## 8단계 - RSSI 기반 위치 갱신 실험

기간: 2026-08-25 ~ 2026-08-31

작업:

- 기준 위치 후보 4~6개 선정
- 각 후보 위치에서 RSSI 패턴 사전 측정
- 버튼 입력 시 현재 RSSI 패턴 측정
- Fingerprinting 또는 구역 Snap 방식으로 위치 후보 선택
- 다변측량은 비교 실험으로만 수행

완료 조건:

- RSSI Fingerprinting 방식의 위치 후보 선택 정확도 측정
- 구역 Snap 방식의 안정성 측정
- 다변측량 방식의 오차와 흔들림 기록
- 최종 시연에 사용할 위치 갱신 방식을 결정

## 9단계 - 그래픽스 Viewer 통합

기간: 2026-09-01 ~ 2026-09-07

작업:

- SIBR Viewer 또는 PC Viewer의 UDP Pose Receiver와 연결
- Viewer의 JPEG TCP Server와 연결
- Orientation 연속 반영
- 버튼 입력 시에만 Position Update 요청
- Position Snap 또는 Fingerprinting 결과를 Camera Position에 반영
- Render/Encode/Network 지연과 임베디드 지연 분리 측정

완료 조건:

- 핸드헬드를 돌리면 LCD 또는 PC 화면 시점이 같은 방향으로 변함
- Position 버튼 미입력 시 위치가 유지됨
- Position 버튼 입력 시 Viewer가 위치를 갱신함
- 네트워크가 느려져도 과거 영상이 장시간 누적되지 않음

## 10단계 - 현장 통합 시험과 안정화

기간: 2026-09-08 ~ 2026-09-12

작업:

- 실제 실내 공간에 고정 노드 배치
- 노드 좌표와 3DGS 좌표 연결
- 실제 RSSI 수집과 Sionna RT 결과 비교
- 2시간 이상 연속 동작 시험
- 장애 주입 시험
- 위치 Snap 시연 절차 작성
- LCD 스트리밍 실패 시 PC 화면 대체 시연 준비

완료 조건:

- 모든 노드의 위치와 RSSI가 백엔드에 정상 표시
- 핸드헬드 방향값이 Viewer 시야 방향에 반영됨
- 버튼 기반 위치 갱신이 동작함
- 가능하면 핸드헬드에서 3DGS + Heatmap 화면 확인
- Wi-Fi 재연결 후 자동 복구
- 2시간 시험 중 치명적 정지 없음
- 성능 측정 결과를 표와 그래프로 정리

## 11단계 - 최종 시연 준비

기간: 2026-09-13 이후

작업:

- Firmware 버전 고정
- 하드웨어 배선과 전원 정리
- 예비 장치 또는 복구 Firmware 준비
- 시연 전 자동 점검 절차 작성
- 발표용 아키텍처와 성능 자료 정리
- 위치 추정 방식의 한계와 선택 근거 정리

---

## 9. 시험 계획

## 9.1 고정 노드 시험

| 시험 | 방법 | 기록 지표 |
|---|---|---|
| RSSI 안정성 | 같은 위치에서 10분 이상 반복 측정 | 평균, 표준편차, 최소·최대 |
| 필터 비교 | Raw, Moving Average, Median 비교 | 변동폭, 반응 지연 |
| 멀티 노드 | 3개 이상 동시 MQTT 전송 | 손실률, 지연, Broker 부하 |
| Wi-Fi 장애 | AP를 1분간 중단 | 감지 및 재연결 시간 |
| Broker 장애 | Broker를 1분간 중단 | Buffer 수, 재전송 결과 |
| 장시간 동작 | 2시간 이상 실행 | Reset, Heap 변화, Queue Overflow |

## 9.2 핸드헬드 시험

| 시험 | 방법 | 기록 지표 |
|---|---|---|
| IMU 기본 동작 | 정지, 좌우 회전, 상하 회전 | 축 방향, 노이즈 |
| IMU 지연 | 장치 회전과 Viewer 반응 비교 | Pose-to-Display 지연 |
| Yaw Drift | 정지 상태 1/5/10분 | 각도 변화 |
| Recenter | 기준 방향 재설정 | 재설정 후 오차 |
| 버튼 Debounce | 연속 클릭 및 길게 누르기 | 중복 이벤트 수 |
| JPEG 품질 | 품질 50/65/80 영상 전송 | 크기, FPS, 화질 |
| 디코딩 성능 | 480×800 또는 800×480 연속 프레임 | 평균·최대 디코딩 시간 |
| LCD 성능 | RGB565 데이터를 8080 병렬로 연속 전송 | 실제 출력 FPS |
| TCP 장애 | 서버 중단 후 재시작 | 재연결 시간 |
| 메모리 안정성 | 2시간 영상 수신 | Heap/PSRAM 변화, Crash |

## 9.3 위치 갱신 시험

| 시험 | 방법 | 기록 지표 |
|---|---|---|
| 수동 Snap | 버튼 입력 시 후보 위치 이동 | 이벤트 지연, 위치 갱신 성공률 |
| RSSI Fingerprinting | 사전 측정 위치와 현재 RSSI 비교 | 후보 위치 정확도 |
| 구역 Snap | 방을 구역으로 나누어 위치 선택 | 구역 분류 정확도 |
| 다변측량 비교 | RSSI를 거리로 변환해 위치 추정 | 평균 오차, 최대 오차, 흔들림 |
| 반복성 | 같은 위치에서 여러 번 버튼 입력 | 선택 위치 일관성 |

## 9.4 전체 시스템 성능 지표

- 고정 노드 측정 주기 편차
- MQTT 메시지 손실률
- 노드별 재연결 시간
- RSSI 필터 적용 전·후 표준편차
- IMU 전송 주기와 UDP Packet Loss
- Recenter 후 방향 오차
- 위치 갱신 요청 지연
- 위치 후보 선택 정확도
- JPEG 평균 Frame 크기
- TCP 수신 시간
- JPEG 디코딩 시간
- LCD 출력 FPS
- Frame Drop 비율
- End-to-End 지연시간
- 장시간 실행 중 Reset 횟수

---

## 10. 위험 요소와 대응

| 위험 | 영향 | 우선 대응 |
|---|---|---|
| LCD 8080 병렬 핀 부족 | LCD, 터치, IMU, 버튼을 동시에 연결하기 어려움 | ESP32-S3-DEVKITC-1-N8R8 핀맵 우선 확정, 터치는 후순위, 필요 시 8-bit 모드 검토 |
| NT35510 드라이버 포팅 지연 | LCD 표시 일정 지연 | 단색/테스트 이미지부터 검증, 7월 중순 Plan B 판단 |
| 480×800 JPEG 디코딩이 10 FPS 미달 | 핸드헬드 시연 품질 저하 | 5 FPS를 1차 목표로 설정, JPEG 품질 조정, Line/Tile Buffer 검토 |
| Wi-Fi 수신, JPEG 디코딩, LCD 전송이 동시에 부하를 유발 | 영상 끊김 또는 Watchdog Reset | 태스크 우선순위 조정, Queue 최소화, Pull 방식 적용, 프레임 드롭 허용 |
| PSRAM 사용량 또는 대역폭 부족 | 화면 깨짐 또는 FPS 저하 | Full Framebuffer 최소화, 반복 할당 제거, JPEG 더블버퍼 + RGB565 Line/Tile 더블버퍼 우선 |
| 순수 6축 IMU 구매 시 Drift와 필터 튜닝 부담 증가 | Viewer 방향 불일치와 개발 지연 | BNO085/BNO080 또는 BNO055 우선 구매, Recenter 기능 필수 |
| 스마트 IMU도 실내 자기장 왜곡을 받을 수 있음 | Yaw 방향이 튀거나 천천히 틀어짐 | 1/5/10분 안정성 시험, Recenter 버튼, 시연 동선 제한 |
| IMU만으로 위치 추정 시도 | 위치 오차 급증 | IMU는 Orientation 전용으로 제한 |
| RSSI 삼각측량 오차 | 위치가 계속 튀거나 잘못된 위치 선택 | Fingerprinting 또는 구역 Snap 우선, 다변측량은 비교 실험으로 분리 |
| Wi-Fi Scan과 MQTT 통신 충돌 | 측정 지연 또는 연결 불안정 | 연결 AP RSSI 조회 우선, Scan 주기 제한, 버튼 입력 시에만 위치 Scan |
| TCP 프레임 누적 | 오래된 화면이 표시됨 | Pull 방식 또는 PC 최신 프레임 유지 방식 적용 |
| MQTT 장애 시 Buffer 폭증 | 메모리 고갈 | 고정 크기 RAM Ring Buffer와 오래된 데이터 Drop |
| 좌표축 불일치 | 장치 움직임과 화면 방향 반대 | 축 정의 문서화와 단위 회전 테스트 |
| 패킷 규격 변경 | 통합 지연 | Version 필드와 공통 Header 사용 |
| 위치 추정 연구 범위 확대 | 핵심 시연 미완성 | 버튼식 위치 갱신 인터페이스까지만 MVP-B로 고정 |

---

## 11. 우선 확정해야 할 사항

다음 사항은 구현 시작 시 팀과 합의해야 한다.

1. 고정 ESP32 보드와 핸드헬드 ESP32-S3-DEVKITC-1-N8R8 보드
2. 구매한 LCD의 핀맵과 연결 방식
   - Waveshare 4inch 480×800 Resistive Touch LCD
   - NT35510
   - 16-bit 8080 Parallel
   - XPT2046 Touch SPI
3. IMU 모듈 모델
   - BNO085/BNO080 우선
   - BNO055 차선
   - 순수 6축 IMU는 후순위
4. ESP-IDF 버전과 Firmware 저장소 구조
5. MQTT Topic, Broker 주소, 인증 방식
6. 측정 주기와 백엔드 Time Window
7. UDP/TCP Port
8. Quaternion 좌표축과 성분 순서
9. JPEG Frame Header와 최대 Payload 크기
10. 핸드헬드 위치 갱신 방식
11. Position Snap 후보 좌표 목록
12. RSSI Fingerprinting을 사용할 경우 사전 측정 위치 수와 측정 방법
13. 다변측량을 비교 실험으로 포함할지 여부
14. LCD 스트리밍 실패 시 PC 화면 기반 대체 시연 방식

특히 ESP32-S3-DEVKITC-1-N8R8 기준 핀맵이 확정되지 않으면 LCD·IMU 성능 검증 일정이 지연될 수 있다. LCD는 이미 구매한 모델을 기준으로 진행하되, IMU는 아직 구매 전이므로 개발 시간을 줄일 수 있는 스마트 IMU를 우선 선택한다. 또한 10~13번이 확정되지 않으면 위치 갱신 기능이 연구 범위로 과도하게 커질 수 있으므로, 초기에는 버튼식 Position Snap을 기준으로 통합한다.

---

## 12. 최종 산출물

### 고정 노드

- ESP32 Firmware 소스
- 노드 설정 파일 또는 설정 Header
- MQTT JSON Schema
- 상태·오류 코드 정의
- 배치 위치표
- RSSI 필터 비교 결과
- 장애 복구 시험 결과

### 핸드헬드

- ESP32-S3-DEVKITC-1-N8R8 Firmware 소스
- ESP32-S3-DEVKITC-1-N8R8 기준 회로 및 핀 연결표
- UDP Control Packet 명세
- TCP Video Frame 명세
- 스마트 IMU 방향 안정성 및 Drift 시험 결과
- 버튼 Position Update / Recenter 동작 결과
- JPEG 품질별 성능 결과
- NT35510 LCD FPS 및 지연시간 결과
- JPEG 더블버퍼 + RGB565 Line/Tile 더블버퍼 성능 결과

### 위치 갱신

- Position Snap 후보 좌표표
- RSSI Fingerprinting 사전 측정 데이터
- 위치 후보 선택 정확도 결과
- 다변측량 비교 실험 결과
- 최종 선택 방식과 선택 근거

### 통합

- 임베디드·백엔드·그래픽스 인터페이스 문서
- 전체 시스템 연결도
- 실행 및 시연 절차
- 장애 발생 시 복구 절차
- 최종 성능 측정표

---

## 13. 구현 우선순위 요약

가장 먼저 해야 할 작업은 다음 네 가지다.

1. **고정 노드 1개의 RSSI → MQTT 전송 경로 완성**
2. **ESP32-S3의 스마트 IMU → UDP → PC 테스트 수신기 연결**
3. **Position Update / Recenter 버튼 이벤트 구현**
4. **ESP32-S3-DEVKITC-1-N8R8의 480×800 JPEG 수신·디코딩·LCD 출력 벤치마크**

이 네 경로가 각각 동작하면 이후에는 멀티 노드, 장애 대응, Position Snap, RSSI Fingerprinting, SIBR Viewer 통합 순서로 확장한다.

위치 추정 알고리즘과 실제 RSSI 기반 Sionna 보정은 핵심 데이터 수집 및 IMU 기반 시야 회전이 안정화된 뒤 진행한다. 현재 졸업과제의 임베디드 MVP 성공 기준은 다음과 같이 정의한다.

```text
MVP-A 성공 기준:
고정 노드가 안정적으로 실측 RSSI를 제공하고,
백엔드가 이를 시간 단위로 묶을 수 있다.

MVP-B 성공 기준:
핸드헬드가 스마트 IMU 기반 방향값을 PC Viewer에 전달하고,
버튼 입력으로 Camera Position 갱신 요청을 보낼 수 있다.

MVP-C 성공 기준:
PC 렌더링 화면을 ESP32-S3가 JPEG로 수신하여
480×800 LCD 또는 가로 800×480 화면에 표시할 수 있다.
1차 목표는 5 FPS 이상, 도전 목표는 평균 10 FPS이다.
```

즉, 핵심은 **고정 RSSI 수집 + IMU 기반 시야 회전 + 버튼식 위치 갱신**이다. RSSI 삼각측량은 최종 필수 기능이 아니라 Fingerprinting 또는 구역 Snap과 비교하는 실험 기능으로 둔다.

---

## 14. 참고 자료

- Espressif ESP32-S3-DevKitC-1-N8R8 보드 문서 및 핀맵
- Waveshare 4inch Resistive Touch LCD / NT35510 / XPT2046 자료
- BNO085/BNO080 또는 BNO055 스마트 IMU 자료
- `2026 졸업과제 기획안(260331).pdf`
- `2026전기_착수보고서_16_이거왜됨연구회_3DGS 기반 공간 데이터 시각화.pdf`
- `3DGS_Sionna_RT_실시간_시각화_파이프라인.md`
