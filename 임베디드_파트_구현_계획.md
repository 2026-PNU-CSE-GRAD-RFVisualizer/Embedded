# 3DGS 기반 공간 데이터 시각화 - 임베디드 파트 구현 계획

작성일: 2026-06-25  
담당: 이나영  
대상 기간: 2026년 6월 말 ~ 9월

## 1. 문서 목적

본 문서는 졸업과제 기획안과 착수보고서에서 정의한 임베디드 역할을 실제 구현 가능한 작업 단위로 구체화한다.

그래픽스 파트의 최신 파이프라인에 맞추어 임베디드 시스템을 다음 두 종류의 장치로 분리한다.

1. **고정 ESP32 RSSI 측정 노드**
   - 실내 여러 위치에서 특정 AP의 RSSI를 반복 측정한다.
   - 측정값을 필터링하고 MQTT로 백엔드에 전송한다.
   - 통신 장애가 발생해도 측정 데이터를 최대한 보존한다.

2. **핸드헬드 ESP32-S3 표시 장치**
   - IMU로 장치 방향을 추정해 PC Viewer에 전송한다.
   - 버튼 입력으로 Camera Position 갱신을 요청한다.
   - PC가 렌더링한 JPEG 화면을 수신·디코딩해 800×480 RGB LCD에 표시한다.

핸드헬드 장치는 3DGS 렌더링이나 Sionna RT 계산을 직접 수행하지 않는 **Thin Client**로 구현한다.

---

## 2. 구현 범위

### 2.1 최소 구현 범위(MVP)

- 고정 ESP32 노드 3개 이상
- 한 개의 고정 AP를 대상으로 RSSI 측정
- FreeRTOS 기반 측정·통신·상태 관리 태스크 분리
- Moving Average 또는 Median Filter 적용
- MQTT 기반 RSSI 및 장치 상태 전송
- Wi-Fi/MQTT 자동 재연결
- 전송 실패 데이터의 제한적 버퍼링 및 재전송
- Watchdog과 장치 상태 코드
- ESP32-S3 + PSRAM 기반 핸드헬드 장치
- IMU Quaternion 생성 및 UDP 전송
- 위치 갱신 버튼과 방향 재중심화 기능
- TCP 기반 JPEG 프레임 수신
- JPEG 디코딩 후 RGB565 변환
- 800×480 RGB LCD 출력
- 평균 10 FPS 목표
- FPS, 지연시간, RSSI 변동, 패킷 손실 및 재연결 시간 측정

### 2.2 MVP 이후 확장 범위

- 고정 ESP32 노드를 이용한 핸드헬드 위치 추정
- RSSI Fingerprinting, 다변측량 또는 구간 기반 위치 Snap 비교
- 고정 노드 수 증가에 따른 확장성 시험
- 실제 RSSI와 Sionna RT 결과의 자동 보정
- 배터리 구동과 전력 관리
- OTA Firmware Update

### 2.3 현재 구현하지 않을 항목

- ESP32에서 3DGS 또는 Sionna RT 직접 실행
- 핸드헬드의 연속 6DoF 위치 추적
- 여러 높이의 3D Volume 데이터 처리
- 고정 노드 기반 위치 추정 알고리즘을 초기 단계에서 확정하는 것

---

## 3. 전체 임베디드 구조

```text
                    [고정 RSSI 측정 노드 N개]

특정 AP RSSI Scan
    ↓
필터링 / 이상치 제거
    ↓
측정 Queue
    ↓
MQTT Publish
    ↓
백엔드 수집·동기화
    ↓
실제 RSSI 검증 데이터


                    [핸드헬드 ESP32-S3]

IMU → Quaternion ───────── UDP ─────────→ PC Viewer
버튼 → Position Update ─── UDP ─────────→ PC Viewer
                                             │
                                             │ 3DGS + Heatmap 렌더링
                                             ↓
RGB LCD ← JPEG Decode ← TCP Frame 수신 ← JPEG Streaming
```

고정 노드의 MQTT 계측 경로와 핸드헬드의 실시간 제어·영상 경로는 분리한다. 한 경로의 장애가 다른 경로를 정지시키지 않도록 각각 독립적으로 구현한다.

---

## 4. 하드웨어 구성 계획

## 4.1 고정 RSSI 측정 노드

필수 구성:

- ESP32 계열 보드
- 안정적인 USB 전원 또는 5 V 어댑터
- 상태 확인용 LED
- 필요 시 리셋 버튼

노드별로 다음 정보를 고정 설정한다.

- `node_id`
- 설치 위치의 실제 좌표
- 측정 대상 AP의 SSID/BSSID
- MQTT Broker 주소
- 측정 주기

AP 식별에는 SSID보다 **BSSID를 우선 사용**한다. 동일 SSID를 사용하는 여러 AP가 있을 때 측정 대상이 섞이는 문제를 방지하기 위해서다.

## 4.2 핸드헬드 표시 장치

기준 구성:

- ESP32-S3
- PSRAM 지원 보드
- 800×480 RGB LCD
- 6축 또는 9축 IMU
- 위치 갱신 버튼
- 방향 재중심화 버튼
- 연결 및 오류 상태 표시 수단
- 안정적인 외부 전원

하드웨어 선정 전에 아래 항목을 먼저 확인한다.

- ESP32-S3와 LCD의 실제 핀 호환성
- RGB LCD가 요구하는 프레임버퍼 크기
- PSRAM 용량과 접근 속도
- JPEG Decoder가 지원하는 입력 형식
- IMU 라이브러리의 Quaternion 출력 여부
- LCD 드라이버와 JPEG 디코더를 동시에 실행했을 때의 메모리 여유

RGB565 800×480 단일 프레임버퍼는 약 768,000 byte가 필요하다. Double Buffer를 사용하면 약 1.5 MB가 필요하므로 PSRAM 사용을 전제로 한다.

---

## 5. 고정 ESP32 노드 Firmware 설계

## 5.1 FreeRTOS 태스크 구성

| 태스크 | 역할 | 우선순위 원칙 |
|---|---|---|
| `RssiMeasureTask` | 특정 AP RSSI 측정, 원시 샘플 생성 | 주기 정확성 우선 |
| `SignalFilterTask` | 평균·중간값·이상치 제거 | 측정 태스크보다 낮게 설정 |
| `MqttPublishTask` | Queue의 측정값과 상태 전송 | 네트워크 지연이 측정을 막지 않도록 분리 |
| `ConnectionTask` | Wi-Fi/MQTT 연결 및 재연결 | 연결 상태 변화 시 동작 |
| `StorageTask` | 전송 실패 데이터 임시 저장 | 저장 공간과 수명 제한 필요 |
| `HealthTask` | Heap, Queue, 오류 횟수, Watchdog 관리 | 낮은 주기로 실행 |

태스크 간 데이터 전달은 공유 전역 변수보다 FreeRTOS Queue와 Event Group을 우선 사용한다.

```text
RssiMeasureTask
    ↓ raw_sample_queue
SignalFilterTask
    ↓ publish_queue
MqttPublishTask
```

MQTT 전송이 지연되더라도 RSSI 측정 태스크가 멈추지 않아야 한다.

## 5.2 RSSI 측정 및 필터링

초기 측정 절차:

1. 대상 BSSID의 RSSI를 일정 주기로 수집한다.
2. 한 번의 결과만 사용하지 않고 짧은 구간의 여러 샘플을 모은다.
3. 비정상 범위와 급격한 단일 튐을 제거한다.
4. 필터링 전·후 값을 모두 시험 로그에 남긴다.

초기 권장 설정:

| 항목 | 초기값 | 조정 기준 |
|---|---:|---|
| 샘플 주기 | 200~500 ms | Wi-Fi 처리 부하 확인 |
| 전송 주기 | 1 s | 백엔드 Time Window와 합의 |
| Moving Average Window | 5개 | 반응성과 안정성 비교 |
| Median Window | 5개 | 순간 이상치 제거 성능 비교 |
| 유효 RSSI 범위 | -100~-10 dBm | 실제 실험 후 확정 |

Moving Average와 Median Filter를 무조건 동시에 적용하지 않는다. 동일 위치 반복 측정 결과를 비교해 더 적합한 방식을 기본값으로 결정한다.

## 5.3 MQTT 데이터 형식

권장 Topic:

```text
rfmap/nodes/{node_id}/measurement
rfmap/nodes/{node_id}/status
rfmap/nodes/{node_id}/event
```

측정 메시지 예시:

```json
{
  "schema_version": 1,
  "node_id": "node-01",
  "seq": 15234,
  "timestamp_ms": 1782384000123,
  "ap_bssid": "AA:BB:CC:DD:EE:FF",
  "rssi_raw_dbm": -62,
  "rssi_filtered_dbm": -60.8,
  "sample_count": 5,
  "x_m": 1.25,
  "y_m": 3.40,
  "z_m": 1.30,
  "status": 0
}
```

상태 메시지 예시:

```json
{
  "schema_version": 1,
  "node_id": "node-01",
  "timestamp_ms": 1782384000123,
  "uptime_ms": 3600000,
  "wifi_rssi_dbm": -48,
  "free_heap_bytes": 180000,
  "publish_queue_depth": 2,
  "buffered_record_count": 0,
  "reconnect_count": 1,
  "error_flags": 0
}
```

`seq`는 메시지 누락과 중복을 판별하는 기준으로 사용한다. `timestamp_ms`의 생성 방식은 백엔드와 합의해야 하며, NTP 동기화 전에는 별도의 `time_synced` 상태를 제공한다.

## 5.4 장애 대응

| 장애 | 동작 |
|---|---|
| Wi-Fi 연결 끊김 | 지수 Backoff 기반 재연결, 측정 지속 |
| MQTT Broker 연결 끊김 | Queue 또는 Ring Buffer에 임시 저장 |
| Buffer 가득 참 | 가장 오래된 데이터부터 폐기하고 Drop Count 증가 |
| 장시간 태스크 정지 | Task Watchdog으로 재시작 |
| 잘못된 AP 측정 | BSSID 불일치 데이터 폐기 |
| 시간 동기화 실패 | 상대 시간과 `time_synced=false` 전송 |
| 전원 재부팅 | Boot Count 및 Reset Reason 상태 메시지 전송 |

재전송 데이터가 최신 실시간 데이터를 계속 밀어내지 않도록, 복구 후에는 최신 데이터와 과거 데이터를 구분해 제한된 속도로 전송한다.

---

## 6. 핸드헬드 ESP32-S3 Firmware 설계

## 6.1 FreeRTOS 태스크 구성

| 태스크 | 역할 |
|---|---|
| `ImuTask` | IMU 읽기, Bias 보정, Quaternion 생성 |
| `ControlTxTask` | 최신 Quaternion과 버튼 상태 UDP 전송 |
| `VideoRxTask` | TCP 연결, Header 및 JPEG Payload 수신 |
| `JpegDecodeTask` | 최신 JPEG 프레임 디코딩 |
| `DisplayTask` | RGB565 Framebuffer를 LCD에 출력 |
| `InputTask` | Position Update 및 Recenter 버튼 Debounce |
| `ConnectionTask` | Wi-Fi, UDP, TCP 연결 상태 관리 |
| `HealthTask` | FPS, Heap, PSRAM, 오류, Watchdog 관리 |

영상 경로는 다음과 같이 구성한다.

```text
TCP 수신
    ↓
JPEG Buffer A/B
    ↓
JPEG Decode
    ↓
RGB565 Framebuffer
    ↓
LCD 출력
```

영상 수신 속도가 디코딩 속도보다 빠르면 과거 프레임을 쌓지 않고 가장 최신의 완성 프레임을 우선한다.

## 6.2 IMU 및 버튼 처리

초기 동작:

- 부팅 직후 정지 상태에서 Gyroscope Bias Calibration
- Quaternion 형태로 방향 전송
- Orientation은 지속적으로 갱신
- Position은 버튼을 눌렀을 때만 갱신 요청
- Recenter 버튼으로 현재 방향을 기준 방향으로 설정

권장 전송 주기:

- IMU 내부 샘플링: 100 Hz 전후
- Quaternion 계산: 50~100 Hz
- UDP 제어 패킷 전송: 30~60 Hz

정확한 주기는 실제 Viewer 지연과 Wi-Fi 부하를 측정한 뒤 결정한다.

Yaw Drift는 구현 전에 특정 필터를 확정하지 않는다. 우선 실제 장치에서 1분, 5분, 10분 정지 시험을 수행하고 재중심화만으로 시연이 가능한지 확인한다.

## 6.3 UDP 제어 패킷

네트워크 바이트 순서와 구조체 Padding 문제를 피하기 위해 필드를 명시적으로 직렬화한다.

권장 논리 구조:

```cpp
struct HandheldControlPacket
{
    uint32_t magic;
    uint16_t version;
    uint16_t flags;
    uint32_t sequence;
    uint64_t timestamp_us;

    float quaternion_x;
    float quaternion_y;
    float quaternion_z;
    float quaternion_w;
};
```

`flags` 예시:

- Bit 0: Position Update 요청
- Bit 1: Orientation Recenter 요청
- Bit 2: IMU Calibration 완료
- Bit 3: 장치 오류 존재

PC Viewer는 순서가 뒤늦게 도착한 UDP 패킷을 폐기하고 가장 큰 `sequence`의 최신 상태만 사용한다.

## 6.4 TCP JPEG 프레임 프로토콜

초기 구현은 `길이 Header + JPEG Payload` 구조로 한다.

권장 Header:

```cpp
struct VideoFrameHeader
{
    uint32_t magic;
    uint16_t version;
    uint16_t header_size;
    uint32_t sequence;
    uint32_t payload_size;
    uint16_t width;
    uint16_t height;
    uint64_t render_timestamp_us;
    uint32_t payload_crc32;
};
```

수신 절차:

1. Magic 값이 나올 때까지 프레임 시작점을 동기화한다.
2. Header 전체를 수신한다.
3. `payload_size`의 상한을 검사한다.
4. JPEG Payload를 완전히 수신한다.
5. CRC32를 검사한다.
6. 정상 프레임만 Decoder Queue에 전달한다.
7. 연결이 끊기면 소켓을 정리하고 재연결한다.

초기 JPEG 조건:

- 해상도: 800×480
- Pixel 출력: RGB565
- 목표: 평균 10 FPS
- JPEG 품질: 50, 65, 80을 각각 벤치마크
- Frame Queue: 1~2개

## 6.5 메모리 관리

PSRAM에는 큰 버퍼를 배치한다.

- JPEG 수신 버퍼 1~2개
- RGB565 Framebuffer 1개 이상
- 필요 시 LCD용 Bounce Buffer

내부 SRAM에는 시간 민감도가 높은 작은 데이터만 배치한다.

- IMU 상태
- UDP 제어 패킷
- Queue Handle
- 연결 상태와 통계

모든 프레임마다 큰 메모리를 동적 할당·해제하지 않는다. 초기화 시 최대 크기의 버퍼를 확보하고 재사용한다.

---

## 7. 파트 간 인터페이스

## 7.1 임베디드 ↔ 백엔드

임베디드가 제공할 항목:

- MQTT Topic 규칙
- 측정·상태 JSON Schema
- `node_id`, `seq`, `timestamp`, 좌표, BSSID
- 오류 코드와 재연결 상태
- 샘플링 및 필터링 방식

백엔드에서 받아야 할 항목:

- MQTT Broker 주소와 인증 방식
- Time Window 크기
- Timestamp 허용 오차
- 메시지 보관 및 중복 처리 정책
- 장치 설정 변경 채널이 필요한지 여부

## 7.2 임베디드 ↔ 그래픽스/PC

임베디드가 제공할 항목:

- UDP Quaternion/Control Packet
- Position Update 및 Recenter Event
- IMU 유효 상태와 Sequence

그래픽스 파트에서 받아야 할 항목:

- JPEG TCP Server 주소와 Port
- UDP Pose 수신 Port
- 확정된 Quaternion 축 방향과 회전 순서
- Camera 좌표계 변환 규칙
- Video Frame Header와 최대 Payload 크기
- Viewer가 사용하는 화면 해상도와 JPEG 형식

통합 전에 다음 좌표계 합의를 문서로 확정해야 한다.

- ESP32 IMU의 전방·우측·상단 축
- Quaternion의 성분 순서
- Right-handed/Left-handed 좌표계
- 초기 자세의 정의
- Recenter 적용 위치

---

## 8. 단계별 구현 계획

## 1단계 - 개발 환경과 하드웨어 검증

기간: 2026-06-25 ~ 2026-07-03

작업:

- ESP-IDF 또는 현재 팀 표준 개발 환경 확정
- 고정 ESP32 보드와 ESP32-S3 보드 구분
- LCD, PSRAM, IMU 단독 예제 실행
- MQTT Broker와 테스트 Publish 확인
- PC에서 테스트 TCP JPEG Server 준비 요청

완료 조건:

- ESP32에서 대상 AP RSSI 출력
- ESP32-S3에서 LCD 단색 및 테스트 이미지 출력
- IMU 원시값 출력
- PSRAM 인식과 사용 가능 용량 확인

## 2단계 - 고정 노드 최소 계측 기능

기간: 2026-07-04 ~ 2026-07-12

작업:

- BSSID 기준 RSSI 측정
- `RssiMeasureTask`, `MqttPublishTask` 분리
- 기본 JSON 패킷 정의
- `node_id`, `seq`, `timestamp` 적용
- 1개 노드 MQTT 전송

완료 조건:

- 1시간 연속 측정 중 태스크 정지 없음
- 서버에서 Sequence 누락률 확인 가능
- 측정 중 MQTT 지연이 발생해도 RSSI Task가 유지됨

## 3단계 - 필터링과 멀티 노드 확장

기간: 2026-07-13 ~ 2026-07-22

작업:

- Moving Average와 Median Filter 비교
- 동일 위치 반복 측정 데이터 저장
- 노드 3개 이상 동시 동작
- 노드 위치와 상태 메시지 추가
- 백엔드 Time Window 동기화 시험

완료 조건:

- 필터 선택 근거를 그래프로 제시 가능
- 노드 3개 이상이 고유 ID로 동시에 전송
- 백엔드에서 동일 시간 구간의 프레임 생성 가능

## 4단계 - 고정 노드 장애 대응

기간: 2026-07-23 ~ 2026-07-31

작업:

- Wi-Fi/MQTT 자동 재연결
- 제한 크기 Ring Buffer
- 재전송 및 Drop Count
- Watchdog
- Reset Reason, Free Heap, Reconnect Count 전송

완료 조건:

- AP 또는 Broker를 1분간 중단한 뒤 자동 복구
- 복구 후 새 데이터 전송 정상화
- Buffer Overflow 시 장치가 멈추지 않고 Drop Count만 증가

## 5단계 - 핸드헬드 LCD·JPEG 단독 벤치마크

기간: 2026-08-01 ~ 2026-08-10

작업:

- TCP 프레임 수신기 구현
- JPEG Buffer와 Decoder 연결
- RGB565 LCD 출력
- JPEG 품질 50/65/80 비교
- PSRAM 사용량, 수신·디코딩·출력 시간 측정

완료 조건:

- PC 테스트 영상이 800×480 LCD에 표시됨
- 평균 10 FPS 근접 여부 확인
- 프레임 손상과 연결 해제 시 자동 복구
- 품질별 프레임 크기와 지연시간 기록

이 단계는 전체 Viewer 통합 전에 반드시 완료한다. 800×480 @ 10 FPS가 장치에서 가능한지 먼저 확인하지 않으면 그래픽스 파트의 스트리밍 설계를 확정할 수 없다.

## 6단계 - IMU 방향 및 버튼 제어

기간: 2026-08-11 ~ 2026-08-20

작업:

- IMU Bias Calibration
- Quaternion 생성
- UDP 제어 패킷 구현
- Position Update와 Recenter 버튼
- Sequence와 Timestamp 추가
- PC 테스트 수신기에서 자세 확인

완료 조건:

- 장치 회전 방향과 PC Camera 회전 방향이 일치
- 버튼 입력이 중복 없이 전달됨
- 1분, 5분, 10분 Yaw Drift 측정값 확보
- UDP 손실 시에도 최신 Orientation으로 복구

## 7단계 - 그래픽스 Viewer 통합

기간: 2026-08-21 ~ 2026-08-31

작업:

- SIBR Viewer의 UDP Pose Receiver와 연결
- Viewer의 JPEG TCP Server와 연결
- Orientation 연속 반영
- 버튼 입력 시에만 Position Update 요청
- Render/Encode/Network 지연과 임베디드 지연 분리 측정

완료 조건:

- 핸드헬드를 돌리면 LCD 화면 시점이 같은 방향으로 변함
- Position 버튼 미입력 시 위치가 유지됨
- Position 버튼 입력 시 Viewer가 갱신 요청을 수신함
- 네트워크가 느려져도 과거 영상이 장시간 누적되지 않음

## 8단계 - 현장 통합 시험과 안정화

기간: 2026-09-01 ~ 2026-09-12

작업:

- 실제 실내 공간에 고정 노드 배치
- 노드 좌표와 3DGS 좌표 연결
- 실제 RSSI 수집과 Sionna RT 결과 비교
- 2시간 이상 연속 동작 시험
- 장애 주입 시험
- 시연 절차 작성

완료 조건:

- 모든 노드의 위치와 RSSI가 백엔드에 정상 표시
- 핸드헬드에서 3DGS + Heatmap 화면 확인
- Wi-Fi 재연결 후 자동 복구
- 2시간 시험 중 치명적 정지 없음
- 성능 측정 결과를 표와 그래프로 정리

## 9단계 - 최종 시연 준비

기간: 2026-09-13 이후

작업:

- Firmware 버전 고정
- 하드웨어 배선과 전원 정리
- 예비 장치 또는 복구 Firmware 준비
- 시연 전 자동 점검 절차 작성
- 발표용 아키텍처와 성능 자료 정리

---

## 9. 시험 계획

## 9.1 고정 노드 시험

| 시험 | 방법 | 기록 지표 |
|---|---|---|
| RSSI 안정성 | 같은 위치에서 10분 이상 반복 측정 | 평균, 표준편차, 최소·최대 |
| 필터 비교 | Raw, Moving Average, Median 비교 | 변동폭, 반응 지연 |
| 멀티 노드 | 3개 이상 동시 MQTT 전송 | 손실률, 지연, Broker 부하 |
| Wi-Fi 장애 | AP를 1분간 중단 | 감지 및 재연결 시간 |
| Broker 장애 | Broker를 1분간 중단 | Buffer 수, 재전송 결과 |
| 장시간 동작 | 2시간 이상 실행 | Reset, Heap 변화, Queue Overflow |

## 9.2 핸드헬드 시험

| 시험 | 방법 | 기록 지표 |
|---|---|---|
| JPEG 품질 | 품질 50/65/80 영상 전송 | 크기, FPS, 화질 |
| 디코딩 성능 | 800×480 연속 프레임 | 평균·최대 디코딩 시간 |
| LCD 성능 | 연속 RGB565 갱신 | 실제 출력 FPS |
| IMU 지연 | 장치 회전과 Viewer 반응 비교 | Pose-to-Display 지연 |
| Yaw Drift | 정지 상태 1/5/10분 | 각도 변화 |
| TCP 장애 | 서버 중단 후 재시작 | 재연결 시간 |
| 메모리 안정성 | 2시간 영상 수신 | Heap/PSRAM 변화, Crash |

## 9.3 전체 시스템 성능 지표

- 고정 노드 측정 주기 편차
- MQTT 메시지 손실률
- 노드별 재연결 시간
- RSSI 필터 적용 전·후 표준편차
- IMU 전송 주기와 UDP Packet Loss
- JPEG 평균 Frame 크기
- TCP 수신 시간
- JPEG 디코딩 시간
- LCD 출력 FPS
- Frame Drop 비율
- End-to-End 지연시간
- 장시간 실행 중 Reset 횟수

---

## 10. 위험 요소와 대응

| 위험 | 영향 | 우선 대응 |
|---|---|---|
| 800×480 JPEG 디코딩이 10 FPS 미달 | 핸드헬드 시연 품질 저하 | JPEG 품질 조정, Decoder 변경, 부분/직접 출력 검토 |
| PSRAM 대역폭 부족 | 화면 깨짐 또는 FPS 저하 | 버퍼 수 최소화, 반복 할당 제거, Bounce Buffer 조정 |
| IMU Yaw Drift | Viewer 방향 불일치 | 재중심화 기능 우선, 필요 시 Magnetometer 또는 Filter 비교 |
| Wi-Fi와 LCD 처리 간 CPU 경합 | 영상 끊김 | 태스크 Core·우선순위 조정, Queue 분리 |
| MQTT 장애 시 Buffer 폭증 | 메모리 고갈 | 고정 크기 Ring Buffer와 오래된 데이터 Drop |
| 좌표축 불일치 | 장치 움직임과 화면 방향 반대 | 축 정의 문서화와 단위 회전 테스트 |
| 패킷 규격 변경 | 통합 지연 | Version 필드와 공통 Header 사용 |
| 위치 추정 연구 범위 확대 | 핵심 시연 미완성 | 버튼식 위치 갱신 인터페이스까지만 MVP로 고정 |

---

## 11. 우선 확정해야 할 사항

다음 사항은 구현 시작 시 팀과 합의해야 한다.

1. 고정 ESP32 및 핸드헬드 ESP32-S3의 정확한 보드 모델
2. RGB LCD 모델과 드라이버 IC
3. IMU 모델
4. ESP-IDF 버전과 Firmware 저장소 구조
5. MQTT Topic, Broker 주소, 인증 방식
6. 측정 주기와 백엔드 Time Window
7. UDP/TCP Port
8. Quaternion 좌표축과 성분 순서
9. JPEG Frame Header와 최대 Payload 크기
10. 핸드헬드 위치 추정의 MVP 포함 여부

특히 1~3번 하드웨어 모델이 확정되지 않으면 LCD·IMU 성능 검증 일정이 지연될 수 있다.

---

## 12. 최종 산출물

### 고정 노드

- ESP32 Firmware 소스
- 노드 설정 파일 또는 설정 Header
- MQTT JSON Schema
- 상태·오류 코드 정의
- 배치 위치표
- RSSI 필터 비교 결과
- 장애 복구 시험 결과

### 핸드헬드

- ESP32-S3 Firmware 소스
- 회로 및 핀 연결표
- UDP Control Packet 명세
- TCP Video Frame 명세
- JPEG 품질별 성능 결과
- IMU Drift 시험 결과
- LCD FPS 및 지연시간 결과

### 통합

- 임베디드·백엔드·그래픽스 인터페이스 문서
- 전체 시스템 연결도
- 실행 및 시연 절차
- 장애 발생 시 복구 절차
- 최종 성능 측정표

---

## 13. 구현 우선순위 요약

가장 먼저 해야 할 작업은 다음 세 가지다.

1. **고정 노드 1개의 RSSI → MQTT 전송 경로 완성**
2. **ESP32-S3의 800×480 JPEG 수신·디코딩·LCD 출력 벤치마크**
3. **IMU Quaternion → UDP → PC 테스트 수신기 연결**

이 세 경로가 각각 동작하면 이후에는 멀티 노드, 장애 대응, SIBR Viewer 통합 순서로 확장한다.

위치 추정 알고리즘과 실제 RSSI 기반 Sionna 보정은 핵심 데이터 수집 및 화면 스트리밍이 안정화된 뒤 진행한다. 현재 졸업과제의 임베디드 MVP 성공 기준은 **고정 노드가 안정적으로 실측 RSSI를 제공하고, 핸드헬드가 장치 방향을 전달하면서 PC 렌더링 화면을 800×480 LCD에 약 10 FPS로 표시하는 것**이다.

---

## 14. 참고 자료

- `2026 졸업과제 기획안(260331).pdf`
- `2026전기_착수보고서_16_이거왜됨연구회_3DGS 기반 공간 데이터 시각화.pdf`
- `3DGS_Sionna_RT_실시간_시각화_파이프라인.md`
