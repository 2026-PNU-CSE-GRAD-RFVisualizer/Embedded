# Handheld 전체 배치 및 배선

## 1. 적용 부품

- ESP32-S3-DevKitC-1-N16R8, 양쪽 22핀, USB-C 2개
- Waveshare 4inch Resistive Touch LCD, NT35510, 2×17핀
- BNO085 breakout, 1×10핀
- 12×12×15 mm 4핀 택트스위치 2개
- 8×12 cm, 42×30홀 만능기판
- 빨강·검정·파랑 연선과 암-암/암-수 듀퐁선

현재 Touch Controller인 XPT2046은 연결하지 않는다. LCD 표시, BNO085 자세, TELEPORT 버튼, HEIGHT 버튼만 배선한다.

## 2. 전선 색상 규칙

| 색상 | 용도 | 예시 |
|---|---|---|
| 검정 | GND 전용 | ESP GND, LCD GND, BNO GND, 버튼 공통 GND |
| 빨강 | 전원 전용 | 5V, 3V3 |
| 파랑 | 신호 전용 | LCD D0~D15, I2C, 버튼 신호 |

주의사항:

- 빨간 선을 버튼 신호로 사용하지 않는다.
- 5V와 3V3가 모두 빨간색이므로 양 끝에 반드시 `5V` 또는 `3V3` 라벨을 붙인다.
- 파란 신호선은 모두 양 끝에 `D0`, `SDA`, `TELEPORT`처럼 신호명을 붙인다.
- 라벨을 붙이지 않은 같은 색 선 여러 개를 한꺼번에 납땜하지 않는다.

## 3. 전체 연결 구조

```text
                         뚜껑
       ┌─────────────────────────────────────┐
       │ LCD 2×17핀                          │
       │   ║ 짧은 암-암 듀퐁선 묶음          │
       │ TELEPORT 버튼 ─ 파랑 TELEPORT ─┐    │
       │ HEIGHT 버튼 ─ 파랑 HEIGHT ─────┼─ 3핀 연결부
       │ 두 버튼 GND ─ 검정 ────────────┘    │
       └─────────────────────────────────────┘
                         │
                         ▼
       ┌─────────────────────────────────────┐
       │ 42×30 만능기판                      │
       │ LCD용 2×17 수핀헤더                 │
       │ ESP32-S3용 2×22 암핀소켓            │
       │ BNO085용 1×10 암핀소켓              │
       │ 버튼용 1×3 수핀헤더                 │
       └─────────────────────────────────────┘
```

BNO085와 버튼은 서로 연결하지 않는다. 둘 다 ESP32-S3에 각각 연결한다.

## 4. 만능기판 좌표와 부품 방향

부품면에서 왼쪽 위 홀을 `X1,Y1`로 정한다.

```text
X: 왼쪽 X1 → 오른쪽 X42
Y: 위쪽 Y1 → 아래쪽 Y30
실물 LCD 설치 기준 비움: Y1~Y2, Y28~Y30
```

> 2026-08-29 실물 확인 결과, 초기 문서의 `P1=X3,Y5` 가정은 틀렸다. 실제 납땜된 LCD 헤더는 `Y3/Y4`이고 `P1=5V`는 `X19,Y3`이다. 아래 좌표는 이 실물 확인값으로 수정한 것이다.

| 부품/커넥터 | 좌표 | 방향 |
|---|---|---|
| LCD 2×17 수핀헤더 | `X3~X19`, `Y3/Y4` | Pin 1/5V는 `X19,Y3`, Pin 2/GND는 `X19,Y4` |
| ESP32 2×22 암핀소켓 J1 | `X4~X25`, `Y9` | USB-C 왼쪽, 안테나 오른쪽 |
| ESP32 2×22 암핀소켓 J3 | `X4~X25`, `Y19` | USB-C 왼쪽, 안테나 오른쪽 |
| BNO085 1×10 암핀소켓 | `X10~X19`, `Y22` | 시험했을 때처럼 핀열이 위쪽, 센서 몸체는 아래쪽 |
| 버튼 1×3 수핀헤더 | `X2`, `Y23~Y25` | 위에서부터 GND, TELEPORT, HEIGHT |

ESP32를 위 방향에서 보았을 때 USB-C가 왼쪽, 안테나가 오른쪽이면 다음과 같이 번호가 증가한다.

```text
Y9  J1:  X25=J1-1  ...  X4=J1-22
Y19 J3:  X25=J3-1  ...  X4=J3-22
```

ESP32-S3 호환 보드는 실크나 보드 Revision이 다를 수 있다. 납땜 전에 실제 보드에 인쇄된 `3V3`, `5V`, `G`, GPIO 번호가 아래 표와 같은지 확인한다.

오른쪽의 빈 공간은 정상이다. ESP32 안테나가 오른쪽을 향하므로 그 주변에는 BNO085, 전선 묶음, 금속 브래킷을 두지 않는다.

## 5. ESP32 소켓의 사용 핀 좌표

| 용도 | ESP 핀 | ESP 헤더 | 만능기판 좌표 |
|---|---:|---:|---|
| 3.3V 분배 | 3V3 | J1-1 | `X25,Y9` |
| 3.3V 예비 | 3V3 | J1-2 | `X24,Y9` |
| LCD D4 | GPIO4 | J1-4 | `X22,Y9` |
| LCD D5 | GPIO5 | J1-5 | `X21,Y9` |
| LCD D6 | GPIO6 | J1-6 | `X20,Y9` |
| LCD D7 | GPIO7 | J1-7 | `X19,Y9` |
| LCD D15 | GPIO15 | J1-8 | `X18,Y9` |
| LCD WR | GPIO16 | J1-9 | `X17,Y9` |
| TELEPORT 버튼 | GPIO17 | J1-10 | `X16,Y9` |
| LCD DC | GPIO18 | J1-11 | `X15,Y9` |
| LCD D8 | GPIO8 | J1-12 | `X14,Y9` |
| LCD D3 | GPIO3 | J1-13 | `X13,Y9` |
| LCD D9 | GPIO9 | J1-15 | `X11,Y9` |
| LCD D10 | GPIO10 | J1-16 | `X10,Y9` |
| LCD D11 | GPIO11 | J1-17 | `X9,Y9` |
| LCD D12 | GPIO12 | J1-18 | `X8,Y9` |
| LCD D13 | GPIO13 | J1-19 | `X7,Y9` |
| LCD D14 | GPIO14 | J1-20 | `X6,Y9` |
| LCD 5V/BL_VCC | 5V | J1-21 | `X5,Y9` |
| 공통 GND | GND | J1-22 | `X4,Y9` |
| BNO GND | GND | J3-1 | `X25,Y19` |
| LCD D1 | GPIO1 | J3-4 | `X22,Y19` |
| LCD D2 | GPIO2 | J3-5 | `X21,Y19` |
| BNO RST | GPIO42 | J3-6 | `X20,Y19` |
| BNO INT | GPIO41 | J3-7 | `X19,Y19` |
| BNO SCL | GPIO40 | J3-8 | `X18,Y19` |
| BNO SDA | GPIO39 | J3-9 | `X17,Y19` |
| LCD D0 | GPIO38 | J3-10 | `X16,Y19` |
| LCD BL | GPIO48 | J3-16 | `X10,Y19` |
| LCD RST | GPIO47 | J3-17 | `X9,Y19` |
| LCD CS | GPIO21 | J3-18 | `X8,Y19` |
| HEIGHT 버튼 | GPIO19 | J3-20 | `X6,Y19` |
| 공통 GND 예비 | GND | J3-21/22 | `X5,Y19` / `X4,Y19` |

GPIO19는 ESP32-S3의 Native USB D-와 공유된다. HEIGHT에 GPIO19를 사용하면 두 USB-C 중 USB-to-UART로 연결된 포트를 Flash/Log에 사용하고 Native USB 포트는 사용하지 않는다.

### 5.1 5V, 3V3, GND 분배 방법

ESP의 전원 핀 하나에서 여러 부품으로 분기하는 것은 가능하다. 하지만 ESP 소켓의 한 납땜점에 연선 3~4개를 겹쳐 납땜하지 않는다. 만능기판의 빈 홀을 전원 분배점으로 사용한다.

현재 배선의 권장 분배:

```text
ESP 5V (X5,Y9)
  └── 빨강 ── 5V 분배점
                ├── 빨강 ── LCD P1 5V
                └── 빨강 ── LCD P25 BL_VCC

ESP GND (X4,Y9)
  └── 검정 ── LCD GND 분배점
                ├── 검정 ── LCD P2 GND
                └── 검정 ── LCD P26 BL_GND

ESP 3V3 J1-1 (X25,Y9) ── 빨강 3V3 ── BNO VCC (X10,Y22)
ESP 3V3 J1-2 (X24,Y9) ── 빨강 3V3 ── LCD P22 RD (X9,Y4)

ESP GND J3-1  (X25,Y19) ── 검정 ── BNO GND (X11,Y22)
ESP GND J3-21 (X5,Y19)  ── 검정 ── 버튼 공통 GND (X2,Y23)
```

ESP의 여러 GND 핀은 보드 내부에서 서로 연결되어 있으므로 LCD, BNO085, 버튼을 서로 다른 GND 핀에 나누어 연결해도 모두 공통 GND다. 이렇게 하면 한 분기점에 너무 많은 선이 몰리지 않는다.

#### 실제 사진의 가장자리 긴 패드 사용

사용자가 제공한 사진 기준으로, 부품면 왼쪽 가장자리에는 긴 은색 패드가 세로로 여러 개 있다. 납땜면으로 뒤집으면 같은 패드가 오른쪽 가장자리에 보인다.

- 긴 패드 하나의 금속 면 전체는 하나의 납땜점으로 사용할 수 있다.
- 위아래의 서로 다른 긴 패드끼리는 기본적으로 연결되어 있지 않다.
- 원형 홀들도 각각 독립되어 있으며 자동으로 전원 Rail이 되지 않는다.

깨끗하게 비어 있는 긴 패드를 다음처럼 정한다.

```text
부품면 왼쪽 가장자리                 납땜면에서는 오른쪽 가장자리

[ 5V ]  빨강: ESP 5V + LCD P1 + LCD P25
[비움]  절연 간격
[ GND]  검정: ESP GND + LCD P2 + LCD P26
[비움]  절연 간격
[3V3 ]  빨강(3V3 라벨): ESP 3V3 + BNO VCC
```

사진에 이미 납이 묻은 긴 패드나 버튼 헤더 바로 옆 패드는 피하고, 깨끗한 패드를 고른 뒤 앞뒤에 유성펜으로 `5V`, `G`, `3V3`를 표시한다. 5V와 3V3 사이에는 최소 한 패드를 비워 둔다.

긴 패드에 연선을 연결하는 순서:

1. 모든 모듈을 소켓에서 뺀다.
2. 연선을 가까운 빈 원형 홀로 한 번 통과시켜 당김 방지 고리를 만든다.
3. 끝 피복을 약 3 mm만 벗기고 꼰 뒤 얇게 납을 먹인다.
4. 사진 2의 납땜면에서 선택한 긴 패드 위에 끝을 눕혀 납땜한다.
5. 같은 전압으로 갈 선들은 같은 긴 패드의 서로 떨어진 위치에 각각 납땜한다.
6. 인접한 긴 패드에 납이나 풀린 구리 가닥이 닿지 않았는지 확인한다.

긴 패드 한 곳에 모든 GND를 몰아도 전기적으로는 맞지만, 선이 너무 많으면 BNO와 버튼은 ESP의 다른 GND 핀에 직접 연결하는 편이 깔끔하다.

만능기판에 별도 분배점을 만들고 싶다면 비어 있는 Y7 행을 다음처럼 사용할 수 있다.

| 분배점 | 좌표 | 뒷면 연결 | 용도 |
|---|---|---|---|
| 5V BUS | `X3~X5,Y7` | 세 패드를 빨간 연선으로 서로 연결 | ESP 5V 입력, LCD P1, LCD P25 |
| LCD GND BUS | `X11~X13,Y7` | 세 패드를 검정 연선으로 서로 연결 | ESP GND 입력, LCD P2, LCD P26 |

연선으로 BUS를 만드는 방법:

1. 연선 피복을 필요한 길이만 벗기고 가닥을 단단히 꼰다.
2. 납을 얇게 미리 먹여 가닥이 풀리지 않게 한다.
3. 기판 뒷면에서 세 패드 위에 놓고 각 패드마다 납땜한다.
4. 주변 신호 패드와 닿지 않는지 확대해서 확인한다.
5. BUS의 각 패드에는 입·출력선 한 가닥씩만 납땜한다.

공중에서 분기해야 한다면 입력선 1개와 출력선 2개를 `Y`자로 꼬아 납땜한 뒤 접합부 전체를 수축튜브로 감싼다. 선을 꼬기만 하고 납땜하지 않거나, 절연 없이 노출된 채로 두지 않는다.

`5V BUS`, `3V3`, `GND`는 절대로 서로 연결하지 않는다. BNO085에는 5V BUS가 아니라 3V3만 연결한다.

## 6. LCD 2×17 커넥터 전체 배선

LCD 핀 번호는 Waveshare 공식 34핀 정의를 사용한다. 만능기판 헤더는 다음과 같이 번호를 붙인다.

```text
Y3:  P33 P31 P29 ... P5  P3  P1
Y4:  P34 P32 P30 ... P6  P4  P2
     X3  X4  X5  ... X17 X18 X19
```

실물 부품면 좌표에서 오른쪽 끝이 P1/P2다. 기판을 납땜면으로 뒤집어 보면 좌우가 반전되므로, 동일한 물리적 P1/P2 납땜점은 사진에서 반대편에 보일 수 있다. 좌우 위치보다 소켓의 실제 같은 핀 납땜점을 기준으로 확인한다.

| LCD 핀 | 만능기판 좌표 | 신호 | 연결 대상 | 색상 |
|---:|---:|---|---|---|
| 1 | `X19,Y3` | 5V | ESP 5V `X5,Y9` | 빨강, `5V` 라벨 |
| 2 | `X19,Y4` | GND | ESP GND `X4,Y9` | 검정 |
| 3 | `X18,Y3` | D0 | GPIO38 `X16,Y19` | 파랑, `D0` |
| 4 | `X18,Y4` | D1 | GPIO1 `X22,Y19` | 파랑, `D1` |
| 5 | `X17,Y3` | D2 | GPIO2 `X21,Y19` | 파랑, `D2` |
| 6 | `X17,Y4` | D3 | GPIO3 `X13,Y9` | 파랑, `D3` |
| 7 | `X16,Y3` | D4 | GPIO4 `X22,Y9` | 파랑, `D4` |
| 8 | `X16,Y4` | D5 | GPIO5 `X21,Y9` | 파랑, `D5` |
| 9 | `X15,Y3` | D6 | GPIO6 `X20,Y9` | 파랑, `D6` |
| 10 | `X15,Y4` | D7 | GPIO7 `X19,Y9` | 파랑, `D7` |
| 11 | `X14,Y3` | D8 | GPIO8 `X14,Y9` | 파랑, `D8` |
| 12 | `X14,Y4` | D9 | GPIO9 `X11,Y9` | 파랑, `D9` |
| 13 | `X13,Y3` | D10 | GPIO10 `X10,Y9` | 파랑, `D10` |
| 14 | `X13,Y4` | D11 | GPIO11 `X9,Y9` | 파랑, `D11` |
| 15 | `X12,Y3` | D12 | GPIO12 `X8,Y9` | 파랑, `D12` |
| 16 | `X12,Y4` | D13 | GPIO13 `X7,Y9` | 파랑, `D13` |
| 17 | `X11,Y3` | D14 | GPIO14 `X6,Y9` | 파랑, `D14` |
| 18 | `X11,Y4` | D15 | GPIO15 `X18,Y9` | 파랑, `D15` |
| 19 | `X10,Y3` | LCD_CS | GPIO21 `X8,Y19` | 파랑, `CS` |
| 20 | `X10,Y4` | DC | GPIO18 `X15,Y9` | 파랑, `DC` |
| 21 | `X9,Y3` | WR | GPIO16 `X17,Y9` | 파랑, `WR` |
| 22 | `X9,Y4` | RD | ESP 3V3 `X24,Y9` | 빨강, `3V3/RD` 라벨 |
| 23 | `X8,Y3` | RST | GPIO47 `X9,Y19` | 파랑, `LCD_RST` |
| 24 | `X8,Y4` | NC | 연결하지 않음 | 없음 |
| 25 | `X7,Y3` | BL_VCC | ESP 5V `X5,Y9` | 빨강, `5V/BL` 라벨 |
| 26 | `X7,Y4` | GND | ESP GND `X4,Y9` | 검정 |
| 27 | `X6,Y3` | BL | GPIO48 `X10,Y19` | 파랑, `BL` |
| 28 | `X6,Y4` | TP_IRQ | 현재 연결하지 않음 | 없음 |
| 29 | `X5,Y3` | TP_CS | 현재 연결하지 않음 | 없음 |
| 30 | `X5,Y4` | TP_SCK | 현재 연결하지 않음 | 없음 |
| 31 | `X4,Y3` | TP_SDI | 현재 연결하지 않음 | 없음 |
| 32 | `X4,Y4` | TP_SDO | 현재 연결하지 않음 | 없음 |
| 33 | `X3,Y3` | 3.3V 대체 전원 | **연결하지 않음** | 없음 |
| 34 | `X3,Y4` | GND | 연결하지 않아도 됨 | 없음 |

LCD 전원은 P1/P2의 5V 방식을 사용한다. 이때 대체 3.3V 전원 입력 P33에는 전원을 넣지 않는다. P25/P26은 Backlight 전원이므로 P25를 같은 5V에, P26을 GND에 연결한다.

LCD와 만능기판 양쪽이 수핀헤더라면 암-암 듀퐁선을 사용한다. 16-bit I80 신호는 가능한 한 10 cm 이하로 짧게 하고, 각 선을 위 표 순서대로 한 개씩 연결하고 연속성 검사를 한다.

## 7. BNO085 1×10 연결

BNO085는 만능기판의 암핀소켓에 직접 꽂고, 소켓 패드에서 ESP 소켓 패드까지 기판 뒷면 연선으로 연결한다.

사용자가 제공한 실제 보드 사진에서 실크 순서를 확인했다. **부품과 글자가 보이는 면을 위로**, 핀열이 위쪽, 센서 몸체가 아래쪽을 향하게 놓으면 왼쪽부터 다음 순서다.

```text
기판 위쪽/ESP 방향

X10   X11   X12       X13       X14  X15  X16  X17  X18  X19
VCC   GND   SCL/SCK   SDA/MISO  AD0  CS   INT  RST  PS1  PS0
 ●     ●      ●          ●       ●    ●    ●    ●    ●    ●   Y22

                 BNO085 센서 몸체
기판 아래쪽
```

| BNO 좌표 | 실제 실크 | 연결 대상 | ESP 좌표 | 색상 |
|---:|---|---|---:|---|
| `X10,Y22` | VCC_3V3 | ESP 3V3 | `X25,Y9` | 빨강, `3V3` 라벨 |
| `X11,Y22` | GND | ESP GND | `X25,Y19` | 검정 |
| `X12,Y22` | SCL/SCK/RX | GPIO40 | `X18,Y19` | 파랑, `SCL` |
| `X13,Y22` | SDA/MISO/TX | GPIO39 | `X17,Y19` | 파랑, `SDA` |
| `X14,Y22` | AD0/MOSI | 연결하지 않음 | 없음 | 없음 |
| `X15,Y22` | CS | 연결하지 않음 | 없음 | 없음 |
| `X16,Y22` | INT | GPIO41 | `X19,Y19` | 파랑, `INT` |
| `X17,Y22` | RST | GPIO42 | `X20,Y19` | 파랑, `BNO_RST` |
| `X18,Y22` | PS1 | 연결하지 않음 | 없음 | 없음 |
| `X19,Y22` | PS0 | 연결하지 않음 | 없음 | 없음 |

이 보드는 `VCC_3V3`라고 명시되어 있으므로 5V를 연결하지 않는다. 기존 I2C 시험에서 성공한 구성대로 `AD0`, `CS`, `PS1`, `PS0`는 외부 배선을 추가하지 않고 보드의 기본 Strap을 유지한다. 사진 1처럼 뒷면을 보고 장착하면 좌우가 반대로 보이므로, 반드시 사진 2의 실크면을 위로 놓고 좌표를 확인한다.

- 현재 기준은 3.3V 전원과 3.3V Logic이다.
- Breakout에 I2C Pull-up이 없다면 SDA와 SCL 각각에 2.2~4.7 kΩ을 사용해 3.3V로 Pull-up한다.
- 센서 본체가 ESP 안테나 바로 아래나 금속 나사 옆에 오지 않게 한다.

## 8. 뚜껑의 버튼 2개 연결

버튼용 1×3 수핀헤더:

| 만능기판 좌표 | 기능 | ESP 연결 | 색상 |
|---:|---|---|---|
| `X2,Y23` | 버튼 공통 GND | ESP GND `X4,Y19` | 검정 |
| `X2,Y24` | TELEPORT | GPIO17 `X16,Y9` | 파랑, `TELEPORT` 라벨 |
| `X2,Y25` | HEIGHT | GPIO19 `X6,Y19` | 파랑, `HEIGHT` 라벨 |

```text
기판 X2,Y23
검정 GND ─────────● 납땜 분기점
                  ├── 검정 A ── TELEPORT 버튼의 GND 쪽 다리
                  └── 검정 B ── HEIGHT 버튼의 GND 쪽 다리

기판 X2,Y24 ── 파랑 TELEPORT ─ TELEPORT 버튼의 반대쪽 다리
기판 X2,Y25 ── 파랑 HEIGHT ─ HEIGHT 버튼의 반대쪽 다리
```

즉, 만능기판과 뚜껑 사이를 지나가는 선은 3가닥이지만 뚜껑 안에서는 검정 GND가 두 갈래가 된다. 검정선 한 가닥만 한 버튼에 연결하고 끝내는 것이 아니다.

검정선 분기 방법:

1. 기판에서 올라온 검정선 끝에 짧은 검정선 2개를 함께 납땜해 `Y`자 형태로 만든다.
2. 세 선이 만나는 납땜부 전체를 수축튜브로 절연한다.
3. 분기된 검정선 하나를 TELEPORT 버튼에, 다른 하나를 HEIGHT 버튼에 납땜한다.

TELEPORT는 누른 채 조준하고 떼는 동작이므로, 손목 방향을 흐트러뜨리지 않고
검지로 계속 누를 수 있는 측면 위치에 둔다. HEIGHT는 실수로 함께 눌리지 않도록
윗면 또는 뒤쪽의 별도 위치에 둔다.

```text
각 4핀 택트스위치를 위에서 본 개념도

GND 쪽 그룹             SIGNAL 쪽 그룹
 A1 ○                       ○ B1
 A2 ○                       ○ B2

A1-A2는 항상 연결되고 B1-B2도 항상 연결된다.
A 그룹과 B 그룹은 버튼을 누를 때만 서로 연결된다.
```

각 버튼마다 검정 GND는 A 그룹의 다리 하나, 파랑 SIGNAL은 B 그룹의 다리 하나에 납땜한다. 실제 스위치의 그룹 방향은 멀티미터 연속성 모드로 확인한다. 같은 그룹의 다리 두 개에 검정과 파랑을 연결하면 버튼을 누르지 않아도 항상 LOW가 되므로 잘못된 배선이다.

두 GPIO는 ESP32 내부 Pull-up을 사용한다. 따라서 버튼에 3.3V나 5V 선은 필요 없으며, 누르지 않으면 HIGH이고 누르면 GND와 연결되어 LOW가 된다.

뚜껑에 버튼을 홀더로 고정하고, 선은 버튼 다리가 아니라 뚜껑 안쪽에 한 번 묶어 당김을 받게 한다. 버튼 다리 납땜부는 개별 수축튜브로 절연한다.

## 9. 하네스가 없을 때 듀퐁선 사용법

### LCD

- LCD와 만능기판 커넥터가 둘 다 수핀이면 암-암 듀퐁선을 쓴다.
- 2×17 플라스틱 하우징이 없어도 개별 암-암 듀퐁선을 꽂아 사용할 수 있다.
- 두 줄을 각각 테이프나 큰 수축튜브로 묶되 P1/P2 방향 표시를 남긴다.
- 사용하지 않는 P24, P28~P34 위치는 비워 두어 핀 번호가 밀리지 않게 한다.

### 버튼

1. 암-암 듀퐁선 3개의 한쪽 끝을 자른다.
2. 남은 암 커넥터 쪽을 기판의 1×3 수핀헤더에 꽂는다.
3. GND 커넥터의 잘린 선에는 검정 연선을 연결한다.
4. 뚜껑 안에서 검정선을 두 갈래로 분기해 두 버튼에 하나씩 연결한다.
5. 나머지 두 커넥터에는 파랑 신호선을 연결한다.
6. 파랑 두 선은 `TELEPORT`, `HEIGHT` 라벨로 구분한다.
7. 모든 연결부와 GND 분기점을 수축튜브로 절연한다.

검정선 분기가 불편하면 버튼 커넥터를 `1×4`로 바꿔 `GND-TELEPORT-GND-HEIGHT` 네 가닥으로 구성해도 된다. 두 GND는 기판 뒷면에서 동일한 ESP GND에 연결한다. 전기적으로는 1×3 공통 GND 방식과 같다.

## 10. 납땜 순서

1. 만능기판에 LCD 수핀헤더, ESP 암핀소켓, BNO 암핀소켓, 버튼 수핀헤더만 먼저 납땜한다.
2. ESP32, BNO085, LCD는 아직 꽂지 않는다.
3. 기판 뒷면에서 GND 연결을 먼저 한다.
4. 3V3와 5V 전원선을 연결하고 서로 Short가 없는지 검사한다.
5. BNO085 신호 4개를 연결한다.
6. 버튼 신호 2개를 연결한다.
7. LCD D0~D15를 번호순으로 연결한다.
8. LCD CS, DC, WR, RD, RST, BL을 연결한다.
9. 멀티미터 검사 후에만 모듈을 소켓에 꽂는다.
10. BNO085만 시험한 뒤 LCD를 추가하고, 마지막으로 버튼을 시험한다.

## 11. 전원 투입 전 필수 검사

ESP32와 모든 모듈을 소켓에서 뺀 상태로 검사한다.

- 5V와 GND가 Short가 아님
- 3V3와 GND가 Short가 아님
- 5V와 3V3가 서로 연결되지 않음
- LCD D0부터 D15까지 순서가 바뀌거나 서로 Short되지 않음
- LCD P22 RD가 GPIO가 아니라 3V3에 연결됨
- LCD P33에는 아무 전원도 연결되지 않음
- BNO085 VCC가 5V가 아니라 3V3에 연결됨
- BNO085의 `AD0`, `CS`, `PS1`, `PS0`에 새 외부 배선이 연결되지 않음
- TELEPORT는 누를 때만 GPIO17과 GND가 연결됨
- HEIGHT는 누를 때만 GPIO19와 GND가 연결됨
- ESP 안테나 오른쪽 주변에 전선 묶음이 지나가지 않음

ESP를 USB-to-UART 포트로 전원 공급한 뒤 5V/3V3 전압을 먼저 측정한다. LCD Backlight를 켰을 때 ESP가 재부팅되거나 5V가 크게 떨어지면 USB 케이블 또는 전원 용량 문제를 먼저 해결한다.

## 12. Firmware 및 기능 주의사항

- 현재 통합 LCD 핀맵은 `LCD D0 → GPIO38`, `LCD RD → 3V3`이다. 구형 LCD 단독 시험의 `D0 → GPIO0`, `RD → GPIO17`과 혼용하지 않는다.
- BNO085는 위치를 측정하지 않고 방향 Quaternion만 제공한다.
- 버튼은 BNO085가 아니라 ESP GPIO 입력이다.
- RFHC v1에서 GPIO17은 `TELEPORT_BUTTON_HELD`, GPIO19는 `HEIGHT_CYCLE_BUTTON_HELD`의 현재 물리 상태로 전송한다.
- 현재 실물로 검증된 것은 BNO085와 LCD의 동시 구동이며, 버튼과 Network 경로까지 합친 단일 Firmware 실물 시험은 아직 남아 있다.

## 13. 참고 자료

- 로컬 핀맵: [`HANDHELD_BNO085_LCD_PINMAP.md`](./HANDHELD_BNO085_LCD_PINMAP.md)
- ESP32-S3 공식 헤더 핀: <https://docs.espressif.com/projects/esp-dev-kits/en/latest/esp32s3/esp32-s3-devkitc-1/user_guide_v1.1.html#header-block>
- Waveshare LCD 공식 34핀 정의: <https://www.waveshare.com/product/4inch-resistive-touch-lcd.htm>
